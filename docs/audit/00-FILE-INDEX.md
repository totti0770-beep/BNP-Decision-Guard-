# 00 — File Index

Every file on disk, excluding `node_modules/`, `.git/`, `dist/`, `build/`, `.next/` and `docs/audit/` itself.
Source of truth: `docs/audit/_inventory_all.txt` (generated by the Phase 0 `find`).

`Status` is updated as files are read; `PENDING` means not yet opened. Per the audit rules, `Role in project`
is left as `—` until the file has actually been read — a role inferred from a filename is not evidence.

| # | Path | Type | Lines | Status | Role in project |
|---|------|------|-------|--------|-----------------|
| 1 | `.env.example` | example | 120 | READ | Documented environment template, 120 lines, nine sections: Database (:1-6), API/JWT (:8-16), Security incl. CORS, rate limits, body limit, lockout and reset-token lifetime (:18-33), Email (:35-49), the dev reset-token escape hatch (:51-56), demo-credential overrides (:58-64), MinIO/S3 (:66-72), AI/RAG incl. provider switches, EMBEDDING_DIM=384 and RAG_MIN_SIMILARITY=0.25 (:74-103), PHI screening with `PHI_MRN_PATTERN` commented out and `RAG_MAX_PER_DOCUMENT=3` (:105-117), and the web API URL (:119-120). |
| 2 | `.github/scripts/audit-critical.mjs` | mjs | 74 | READ | 74 lines. The hard audit gate for both CI trees (root workspaces, and apps/mobile which is not a workspace and so is invisible to the root audit). Reads `npm audit --json` and decides on `metadata.vulnerabilities.critical` rather than relying on `npm audit --audit-level=critical`'s exit code (:45-75). The header records why (:6-29): that command calls the registry's legacy "quick" audit endpoint, which npm announces is being retired and which answered `400 Invalid package tree` on an unchanged lockfile `npm ci` had installed cleanly seconds earlier in the same job — and npm then exits 1 on the transport error, which the gate cannot tell apart from a real critical finding. It also fails when the audit could not run at all (:47-51, :53-57, :59-63), because silently passing when the advisory data never arrived is the one outcome worse than a false red. Verified by mutation: a stubbed critical count, a stubbed audit error and a lockfile-less directory each exit 1. |
| 3 | `.github/workflows/ci.yml` | yml | 233 | READ | CI, six parallel jobs on push and PR to every branch (:3-7): `security` (npm audit, hard-fail critical :22, non-blocking high :28), `lint` (:30-45), `api` (pgvector service, build, unit tests, migrations, creates `bnp_e2e` via the `pg` client, runs e2e :47-106), `web` (:108-122), `smoke` (docker compose up, health polling, Playwright, screenshot upload, teardown :124-181) and `mobile` (separate `npm ci` in apps/mobile, typecheck, tests, own audit gate :183-221). No `needs:` anywhere. |
| 4 | `.gitignore` | gitignore | 13 | READ | Root ignore list, 13 entries: build output (`node_modules/`, `dist/`, `build/`, `.next/`, `.expo/`, `coverage/`), `.env` and `.env.local` variants, `apps/api/uploads/` and `*.tsbuildinfo` (:1-13). |
| 5 | `CLAUDE.md` | md | 254 | PENDING | — |
| 6 | `README.md` | md | 508 | PENDING | — |
| 7 | `REPO-DISCOVERY.md` | md | 2117 | PENDING | — |
| 8 | `SECURITY.md` | md | 321 | PENDING | — |
| 9 | `apps/api/.gitignore` | gitignore | 2 | READ | 2 lines: `eval-report.md` and `field-eval-report.md`. Both are generated evaluation reports, gitignored on purpose — a committed copy goes stale silently, which is the failure mode they exist to catch. |
| 10 | `apps/api/eval/README.md` | md | 98 | READ | 98 lines, the staff-facing guide. Rule one is a section heading: **never write a question by reading a policy** — a question written from a document guarantees the wording matches, which is why the gold set is a regression detector rather than a measurement (:6-16). States outright that the shipped starter is engineering-authored, exists so the runner has something to run, and that **no figure taken from it belongs in a report to anyone** (:18-26). Documents the eight fields in a table and the four the loader rejects, restating why: whoever asks what staff look up does not know which page holds the answer, and if they do, they wrote the question from the page (:49-53). Calls `paraphraseOf` the highest-value optional field and explains that nobody has to know the right answer to see two wordings disagree (:55-60). Closes on the division that is the whole design: the runner produces the paperwork, not the verdict (:89-98). |
| 11 | `apps/api/eval/field-set.starter.jsonl` | jsonl | 69 | READ | 69 lines. The shipped starter cases, opening with a seven-line `#` comment block declaring that every case is engineering-authored, written by whoever built the platform rather than ward staff, and that no coverage figure taken from them means anything clinically. Cases carry `id`, `question`, `language`, `provenance`, `intent`, `topic` and — on paraphrase pairs — `paraphraseOf`, including Arabic/English pairs such as `out-antivenom-ar` → `out-antivenom`. No case carries `corpus`, which is why none of their intents is gated (asserted in `field-set.spec.ts:137-139`). |
| 12 | `apps/api/field-eval-report.md` | md | 138 | GENERATED-SKIPPED | Untracked build output produced by running the field-set eval locally in this session; gitignored via apps/api/.gitignore. Not part of the source tree. |
| 13 | `apps/api/jest-e2e.config.js` | js | 26 | READ | 26 lines. The integration config, deliberately separate from the unit config in `package.json`: that one has `rootDir: "src"` and so never picks these up, and the docblock names the reason these files live outside `src/` at all — `.e2e-spec.ts` would otherwise match the unit suite's `.*\.spec\.ts$` pattern (:5-8). `maxWorkers: 1` because every spec shares one database and truncates it on setup, so parallel workers would race (:10-11). `setupFiles` points at `test/support/env.ts` with the reason stated: env must be set before `config/env.ts` is imported, since `isProduction` binds at module load and `buildDataSourceOptions()` reads process.env directly (:21-23). |
| 14 | `apps/api/package.json` | json | 77 | READ | 77 lines. Scripts expose the two suites separately (`test` = jest with the inline unit config, `test:e2e` = the standalone config) plus `test:eval` and `test:eval:field`, both of which set `EVAL_REPORT=1` and target one spec by path pattern, and `eval:field` for the HTTP runner. The inline `jest` block (:64-76) is the unit config: `rootDir: "src"` and `testRegex: .*\.spec\.ts$` — the pairing that keeps the e2e specs out. Dependencies pin NestJS 11, TypeORM 0.3, `pg`, `multer` 2, `nodemailer` ^10.0.10 (raised during this audit to clear its advisories), `otplib`, `pdf-parse` and `pdfkit`. |
| 15 | `apps/api/src/analytics/analytics.module.ts` | ts | 76 | READ | Service, controller and module in one file. `overview()` runs three raw SQL queries (:74-103): a twelve-counter scalar row, a 14-day question histogram, and documents by category. Computes `refusalRate` to one decimal (:111). `GET /analytics/overview` behind `ANALYTICS_READ` (:125-129). |
| 16 | `apps/api/src/app.module.ts` | ts | 67 | READ | Root module, 67 lines. Imports TypeORM from `buildDataSourceOptions()` (:29), `ScheduleModule` for the expiry cron (:30), Throttler from `loadEnv().rateLimit` (:31-36) and the thirteen feature modules (:37-49). `HealthController` is the only controller declared here (:51). Global providers in order: `ThrottlerGuard` → `JwtAuthGuard` → `PermissionsGuard` → `PhiScreenGuard` → `AuditInterceptor` (:55-64); the comment at :58-62 states why the PHI guard runs last among guards but still before interceptors and the pipe, and :53-54 why throttling runs first. |
| 17 | `apps/api/src/approval/approval.service.spec.ts` | ts | 220 | READ | 220 lines, 3 describes. The e2e suite walks the happy path through real HTTP; what it cannot do cheaply is enumerate the *illegal* moves, and those are the clinically important ones because each is a route by which unapproved content could reach ACTIVE and be cited (:6-15). The spec keeps its own copy of the legal-transition table (:20-33), generates every (status, action) pair the matrix disallows, asserts there are more than twenty of them, and for each asserts a `BadRequestException` **and** that the status is unchanged, no history row was invented, and nothing was persisted (:144-169). `index()` is pinned as INDEX+ACTIVATE in one call writing two history rows, `deactivate()` is asserted to drop the vectors so the corpus cannot outlive the status (:135-141), indexing a DRAFT is asserted never to run the embedding pipeline on unapproved content (:171-178), and EXPIRE is pinned as a first-class transition attributed to no person (:181-211). |
| 18 | `apps/api/src/approval/approval.service.ts` | ts | 175 | READ | The lifecycle state machine. `TRANSITIONS` (:17-30) lists the statuses each action may leave from; `transition()` (:51-86) is the only path a status may change by, writing the document, a `document_approvals` row and an audit event together. `actor` is nullable because the expiry cron is not a person (:43-50). `expire()` (:101-106) routes through the same machine — the comment records that it previously set `status = EXPIRED` directly and wrote only an audit row, invisible to NURSING_KNOWLEDGE_MANAGER, who owns the lifecycle but holds `documents:read` and not `audit:read` (:88-100). `index()` (:132-149) re-checks `TRANSITIONS[INDEX]` *before* the embedding pipeline so an unapproved document never burns provider quota (:134-144), then performs INDEX and ACTIVATE as two recorded transitions. `deactivate()` removes the vector index after the status change (:152-157). |
| 19 | `apps/api/src/audit/audit.controller.ts` | ts | 22 | READ | One route, `GET /audit-logs` behind `AUDIT_READ` (:10-11), taking action/actorEmail/resourceType plus the shared `PAGE_INT` pipe for limit and offset. |
| 20 | `apps/api/src/audit/audit.module.ts` | ts | 14 | READ | 14 lines. `@Global()` (:7) so `AuditService` is injectable everywhere without each module importing it — which is what lets domain services emit semantic events and `AllExceptionsFilter` record `ERROR:UNHANDLED`. Registers `AuditLog`, the service and `AuditController`. |
| 21 | `apps/api/src/audit/audit.service.ts` | ts | 61 | READ | `record()` is fire-and-forget by design — `void this.repo.save(...)` with a `.catch` that logs (:26-41), so an audit failure can never break a clinical flow. This is also why e2e specs must settle before reading `audit_logs`. `find()` filters by action, actor email (ILIKE) and resource type, capping `limit` at 200 (:44-60). |
| 22 | `apps/api/src/auth/account-security.spec.ts` | ts | 318 | READ | 318 lines, 3 describes. Sets `AUTH_MAX_FAILED_ATTEMPTS` and deletes `NODE_ENV` at the top of the file, before any import, because `AuthService` reads them through `loadEnv()` on first call (:1-5). Lockout: locks after the threshold, blocks even a correct password while locked, clears on success. Password reset carries the security-sensitive half — the token is never returned by default, with the regression noted that it used to be returned whenever `NODE_ENV !== 'production'` and that NODE_ENV is unset in the shipped k8s case, so knowing an email would have been enough to take over an account (:93-103); the opt-in is refused in production even when the flag is set; an unknown email sends no mail and reveals nothing; and delivery is asserted **not** awaited, because awaiting it would make a request for a real account take an SMTP round trip longer than one for an address that does not exist — an enumeration oracle in the timing rather than the body (:173-187). MFA: enrolment stores a secret but leaves MFA off until a code is verified, re-enrolment while enabled is refused because it would leave login verifying against a secret the user's app never saw, and disable requires the account password so a stolen access token alone cannot strip the second factor (:284-294). |
| 23 | `apps/api/src/auth/auth.controller.ts` | ts | 133 | READ | Nine routes, 133 lines. Seven DTOs (:20-49) all `class-validator`-checked. `AUTH_THROTTLE` (:13-18) applies the stricter per-IP credential limit to five public routes and to the three MFA-management routes. Five are `@Public()`: login, refresh, mfa/verify, forgot-password, reset-password. Four are authenticated with no permission (logout, mfa/enroll|enable|disable) and each acts only on `user.userId` from the JWT (:102-106). :62-67 records that public self-registration was removed deliberately — it handed anyone who could reach the API a NURSE_USER account over the approved corpus. |
| 24 | `apps/api/src/auth/auth.module.ts` | ts | 26 | READ | Wires `TypeOrmModule.forFeature([User])`, Passport, and `JwtModule.registerAsync` with a factory so the signing secret resolves through the same `loadEnv()` path the strategy verifies with (:53-57). Providers: AuthService, JwtStrategy, DemoAccountGuardService (:60). |
| 25 | `apps/api/src/auth/auth.service.ts` | ts | 431 | READ | 431 lines, the security core, read in full. `validateUser` checks active, then the lockout window before the password (:44-65). `registerFailedAttempt` locks at the configured threshold and audits (:69-84). `issueTokens` stamps the refresh token with `tv: user.tokenVersion` (:108). `login` returns a 5-minute half-authenticated `mfa` token when MFA is on (:127-144). `verifyMfa` (:158-190), `refresh` rejecting a superseded `tv` (:209-210), `logout` bumping `tokenVersion` (:219-230). `forgotPassword` (:251-284) returns the same shape regardless of whether the account exists, does not await delivery so response timing cannot distinguish them (:245-249, :269), and gates the dev token return on `AUTH_DEV_RETURN_RESET_TOKEN === 'true' && !isProduction` (:280) — the comment records that keying on `NODE_ENV !== 'production'` alone failed open because NODE_ENV is unset in the shipped manifests. `resetPassword` requires the token's `tv` to match and bumps it again (:309-316). MFA enrolment is deliberately two-step: `enrollMfa` never sets `mfaEnabled` (:326-364), `enableMfa` proves possession first (:370-395), `disableMfa` requires the password even though the caller is authenticated (:397-430). |
| 26 | `apps/api/src/auth/demo-account-guard.service.spec.ts` | ts | 163 | READ | 163 lines, 8 cases, each re-importing the guard through an isolated module registry because `isProduction` is a module-load constant (:4-6). Beyond the positive case — account disabled, `isActive` false, `tokenVersion` bumped from 3 to 4, audit row written — three cases guard the false-positive direction, which the comments call the important one: a guard that disables correctly-provisioned accounts is worse than no guard, being an outage with a security label on it that fires every boot (:77-79). A rotated password, a `SEED_PASSWORD_` override, a non-production environment and `ALLOW_DEMO_ACCOUNTS=true` each leave the account untouched with no audit row. An already-disabled account is skipped rather than re-audited, all seven accounts are swept rather than just the first, and a database failure is asserted not to block startup (:157-162). |
| 27 | `apps/api/src/auth/demo-account-guard.service.ts` | ts | 118 | READ | 118 lines. Runs at every production boot (:46-58) and disables any account still using a password published in this repository. Compares against the shipped literal only (:78), so a rotated account — including one seeded from a `SEED_PASSWORD_*` override — cannot false-positive (:29-32). Bumps `tokenVersion` as well as `isActive`, so a session established before the sweep cannot be extended past it (:83-85). `ALLOW_DEMO_ACCOUNTS=true` opts out with a loud error naming the consequence (:64-72). A failure is caught and logged rather than taking the API down, because doing otherwise would hand an attacker a denial of service via the protective control itself (:50-56). Logs how many active accounts remain, and names `create-admin.js` when the answer is zero (:104-115). |
| 28 | `apps/api/src/auth/jwt.strategy.ts` | ts | 37 | READ | Passport JWT strategy. Secret resolves through `loadEnv().jwt.secret` (:21) — the comment (:15-20) records that an inline fallback here resolved to this public repository's literal whenever JWT_SECRET was unset, which only fail-fasts in production. `validate()` rejects any token whose `type` is not `access` (:26-28) and derives permissions from the RBAC matrix, never the database (:34). |
| 29 | `apps/api/src/chat/chat-diagnostics.spec.ts` | ts | 67 | READ | 67 lines, 3 cases. Pins who may see retrieval internals behind a refusal: a nurse gets no `diagnostics` property at all (:43-47), a knowledge manager gets the whole object (:49-57). The third case pins the *reason* rather than the behaviour — `ANALYTICS_READ` is absent from the nurse's permissions and present in the manager's (:59-66) — so a change to the RBAC matrix breaks this test rather than silently widening the exposure. |
| 30 | `apps/api/src/chat/chat.controller.ts` | ts | 73 | READ | Four routes, 73 lines. `AskDto` (:21-26), `ReviewDto` restricted to APPROVED/FLAGGED (:28-30). `POST /chat/ask` is PHI-screened on `question` — described at :38-39 as the field the whole control exists for, free text typed at the bedside and persisted verbatim to `ai_questions.question`. History is scoped to the caller; the two review routes need `AI_REVIEW_ANSWERS`. All three list routes use `PAGE_INT`. |
| 31 | `apps/api/src/chat/chat.module.ts` | ts | 14 | READ | 14 lines. Registers `AiQuestion`, `AiAnswer` and `Citation`, imports `RagModule` for the governed chain, exports `ChatService`. |
| 32 | `apps/api/src/chat/chat.service.ts` | ts | 214 | READ | Persists the question before asking (:39-47), then the answer with its citations and `latencyMs` (:51-74), and audits `AI:ANSWER` or `AI:ANSWER_REFUSED` (:76-89). Diagnostics are returned **only** to an actor holding `analytics:read` (:103-105) — a nurse gets the refusal, a knowledge manager gets the retrieval internals needed to tell a corpus gap from an over-tight threshold. `history()` is scoped to the caller (:110-113). `listAnswersForReview()` is deliberately across all nurses and excludes refusals, since a refusal carries nothing to review (:149-164). Assistant flavours constrain retrieval to a governed category (:11-14). |
| 33 | `apps/api/src/common/decorators.ts` | ts | 48 | READ | Five exports: `Public()`/`IS_PUBLIC_KEY` (:8-9), `Permissions(...)`/`PERMISSIONS_KEY` (:11-13), `ScreenForPhi(spec)`/`PHI_SCREEN_KEY` with the `PhiScreenSpec` shape (:15-35), the `AuthenticatedUser` interface (:37-43) and the `CurrentUser` param decorator reading `req.user` (:45-48). |
| 34 | `apps/api/src/common/filters/all-exceptions.filter.ts` | ts | 78 | READ | Uniform error envelope, 78 lines. `@Catch()` with no argument (:18) so it handles every throwable, not only `HttpException`. Status ≥ 500 is logged with its stack (:41-44), recorded as `ERROR:UNHANDLED` in the audit trail with method, path, status and message (:45-57), and in production the client message is replaced with a generic string (:58). `HttpException`s keep their client-safe message (:35-38). The response body carries `message` and `error` with the same value (:70-76), and :61-69 records why: the filter originally emitted only `error`, while Nest's own envelope — and therefore this repo's web and mobile fetch wrappers — reads `message`, so no client-safe error ever reached a user; a duplicate email surfaced in the browser as "Request failed (400)". |
| 35 | `apps/api/src/common/guards/jwt-auth.guard.ts` | ts | 20 | READ | Extends passport's `AuthGuard('jwt')`; short-circuits to true when `IS_PUBLIC_KEY` is set on the handler or class (:13-17), otherwise delegates (:18). 20 lines, no other behaviour. |
| 36 | `apps/api/src/common/guards/permissions.guard.spec.ts` | ts | 120 | READ | 120 lines, 2 describes. The first walks the matrix through the guard: a nurse may ask the AI but not approve documents and not download source PDFs (the copy-protection rule), only the pharmacist approves dose formulas, an auditor reads audit logs but cannot ask, unauthenticated requests are refused, and `@Public()` passes through (:27-84). The second pins the architectural claim that `rbac.ts` is the only source of truth: a role that exists solely as a database row authorizes nothing (:87-103), no permission exists that would authorize editing role permissions — so removing those endpoints left no orphan capability (:105-109) — and assigning users to roles still works, because roles travel in the JWT (:111-119). |
| 37 | `apps/api/src/common/guards/permissions.guard.ts` | ts | 42 | READ | Reads `PERMISSIONS_KEY` and compares against `user.permissions` (:33-34) — the JWT-derived set, never the database. Passes when public (:16-20) or when no permission is required (:26); throws `ForbiddenException('Not authenticated')` with no user (:31) and names the missing permissions in the message (:36-38). |
| 38 | `apps/api/src/common/guards/phi-scanner.spec.ts` | ts | 162 | READ | 162 lines, 5 describes. Covers the shared PHI pattern set, and lives in the API suite because `packages/shared` has no jest project of its own (:6-8). The header states why the negative half is not a formality: a screen that rejects "جرعة 10 مج" gets reported as broken on day one and switched off by the end of the week, leaving no screen at all (:10-14). Twelve true positives including Arabic-Indic digit folding so switching keyboards is not a bypass (:37-41); seventeen false-positive cases drawn from things a nurse would actually type — doses, volumes, concentrations, policy codes, `q6h`, duration ranges, a bare year — each asserted to return `[]` (:58-82). One case is labelled `KNOWN FALSE POSITIVE` and asserts the *wrong* answer deliberately (a ten-digit batch number starting 1 or 2), pinning the accepted trade-off as a decision on record rather than a surprise (:93-99). The METADATA profile is asserted to allow the dates and names a change note is *for* while still catching identifiers (:102-119), the MRN pattern is asserted inert until configured (:122-147), and the last case pins the load-bearing property that the return type cannot carry content (:154-161). |
| 39 | `apps/api/src/common/guards/phi-screen.guard.spec.ts` | ts | 254 | READ | 254 lines, 6 describes. Unit contract for the guard; the end-to-end proof that nothing reaches the database is in `test/phi-screening.e2e-spec.ts` (:6-11). Non-screened routes, unlisted fields and non-string values pass through — the last because guards run before the `ValidationPipe`, so rejecting a number here would produce a PHI message for what is really a schema error (:66-77). The central case is titled `THE RULE`: it serialises the **entire** audit payload and asserts the rejected text appears nowhere in it, rather than inspecting the fields the test happens to know about, so a future field carrying content would fail (:123-138). Also pins one record per request however many fields are dirty, category breakdown so a false-positive pattern is identifiable, query-string screening for `/rag/search`, the profile split, and that a malformed `PHI_MRN_PATTERN` throws rather than silently screening nothing (:245-253). |
| 40 | `apps/api/src/common/guards/phi-screen.guard.ts` | ts | 107 | READ | The PHI screen. Reads the route's `@ScreenForPhi` spec (:52-56), screens the named body and query fields with `scanForPhi` (:63-78), skipping non-strings because guards run before the ValidationPipe (:70-73). On a hit it records `SECURITY:PHI_BLOCKED` with route, **categories** and profile — never content (:87-99) — logs categories only (:100-103), and throws `BadRequestException(PHI_REJECTION_MESSAGE_AR)` (:105). The docblock (:19-41) states the structural property: a guard throws before the interceptor chain, so `AuditInterceptor` never runs on a rejected request. |
| 41 | `apps/api/src/common/interceptors/audit.interceptor.ts` | ts | 47 | READ | Records every mutating request (`POST/PATCH/PUT/DELETE`, :10,23) except `/auth/*`, which the auth service audits itself to avoid recording credentials (:24-25). Writes actor, `HTTP:<method>:<route>`, outcome, duration and `req.params` (:36-45) — **the request body is never included**, which is what makes the PHI guard's claim hold for routes that are screened and for those that are not. |
| 42 | `apps/api/src/common/logging/json-logger.service.spec.ts` | ts | 70 | READ | 70 lines, 5 cases. The header records that the argument shapes were read out of `@nestjs/common`'s `logger.service.js` rather than guessed (:3-9): a bound-context Logger appends its context as the trailing param, and for `error` inserts an `undefined` placeholder when no stack was given. Cases cover the stdout/stderr split, that placeholder, a real stack kept separate from the message, an `Error` passed directly as `openai-http.ts` does, and a non-string message serialised rather than printed as `[object Object]`. |
| 43 | `apps/api/src/common/logging/json-logger.service.ts` | ts | 82 | READ | One JSON object per line; error/fatal to stderr, everything else to stdout (:148-149). Mirrors Nest's own convention of passing the bound context as the last optional param and an undefined stack placeholder before it (:127-137), so every existing `new Logger(X)` call site works unchanged. Installed once via `app.useLogger()`. |
| 44 | `apps/api/src/common/pagination.ts` | ts | 23 | READ | The shared `PAGE_INT` pipe for every limit/offset parameter. Added by this audit; see the fix commit 68f9511. |
| 45 | `apps/api/src/config/data-source.ts` | ts | 46 | READ | TypeORM options shared by the Nest app and the standalone migrate script. Credentials come from `loadEnv()` (:25) — the comment (:11-23) records why: the container runs migrate.js before main.js, so this was the one entrypoint touching production secrets without the fail-fast. Migrations are listed explicitly (:34-40), `synchronize: false` (:41). |
| 46 | `apps/api/src/config/env.spec.ts` | ts | 399 | READ | 399 lines, 6 describes, read in three chunks. Each describe reloads the module through `jest.resetModules()` because the env is cached and `isProduction` binds at load. Production fail-fast is pinned per secret, including that log-only mail is degraded rather than fatal while an explicitly chosen smtp with no host throws. `NODE_ENV` validation asserts five near-miss values (`Production`, `prod`, `staging`, `PRODUCTION`, `productionx`) refuse to boot while unset still means development. `ragMinSimilarity` carries the regression pin with the mechanism in the comment — `parseFloat('abc')` was NaN, `score >= NaN` is always false, so the assistant refused every question indistinguishably from an empty corpus with nothing logged (:244-247) — and separately refuses values outside [0, 1]. The openai-key describe pins that a whitespace-only key counts as missing, which is how it actually happens in a dashboard (:309-318), while `mock` and unset providers are left alone because the offline path is why this platform runs with no API key at all (:327-334). The RAG integer describe asserts a fractional value is refused rather than truncated: `parseInt('2.7')` was 2, a silent reinterpretation of what the operator wrote (:380-385). |
| 47 | `apps/api/src/config/env.ts` | ts | 386 | READ | 386 lines, read in three chunks (390 before the duplicated comment block noted below was removed). The single secret-resolution path. `validatedNodeEnv()` (:34-46) rejects an unrecognised `NODE_ENV` because that one value gates the secret fail-fast, CORS fail-closed, 5xx suppression, the reset-token refusal and the seed refusal together. `ragInt()` (:90-101) and `ragMinSimilarity()` (:107-122) validate the four RAG knobs, with the docblocks recording exactly how each failed silently before (:68-89, :50-67). `phiMrnPattern()` (:161-175) throws on a malformed pattern and strips `g`/`y` because `RegExp.test` on a sticky pattern advances `lastIndex`. `jwtLifetime()` (:177-188). `required()` (:190-201) fails production boot on missing-or-default. `loadEnv()` (:252-386): NODE_ENV first, then five secrets (:260-267), mail policy (:274-284), the openai-key fail-fast (:299-308), the four RAG knobs and the PHI pattern called for their side effects (:312-319), CORS where production defaults to an empty list (:321-326), then the cached `AppEnv` (:328-384). A duplicated three-line comment after :267 was found during this pass and removed. |
| 48 | `apps/api/src/documents/documents.controller.ts` | ts | 209 | READ | Thirteen routes, 209 lines — the largest controller. Three DTOs (:27-44). Upload is capped at 25 MB to match the web screen's advertised limit, which it used to contradict (:60-65). `GET /documents/inventory` is declared before `@Get(':id')` because Express matches in declaration order (:86-97). Six routes carry `@ScreenForPhi` (:54, :110, :148, :162, :176, :199), five of them on `comment` with the note that the value lands in both `document_approvals.comment` and the audit metadata beside it. Lifecycle transitions delegate to `ApprovalService`; `limit`/`offset` use the shared `PAGE_INT` (:80-81). |
| 49 | `apps/api/src/documents/documents.module.ts` | ts | 20 | READ | Registers Document, DocumentVersion and DocumentApproval, imports RagModule, and hosts the approval workflow alongside documents — controllers `[DocumentsController]`, providers `[DocumentsService, ApprovalService, InventoryService]` (:122-130). |
| 50 | `apps/api/src/documents/documents.service.spec.ts` | ts | 94 | READ | 94 lines, 2 describes. `isPdf` accepts a real signature and one preceded by junk bytes as readers do, and rejects PHP, a Windows executable, empty input, and a signature buried past the scanned prefix (:35-53). The upload gate is then tested both ways round: a non-PDF spoofing the PDF Content-Type and a genuine PDF sent as `image/png` are both rejected *and* storage is asserted never to have been called (:56-81) — the mimetype comes from the client's multipart header and proves nothing on its own (:58-59). |
| 51 | `apps/api/src/documents/documents.service.ts` | ts | 250 | READ | `isPdf()` (:20-22) scans the first 1024 bytes for `%PDF-` rather than demanding offset 0. `upload()` checks the client-supplied mimetype **and** the magic bytes, with a comment stating the header is a usability check and the magic number is the security one (:71-77). Re-upload creates a new version, resets the document to DRAFT and clears `approvalDate`/`approvedBy` (:87-127), so a new version must be re-approved before it can be cited. `findAll` caps `limit` at 200 (:188). `update()` records a from/to for title and expiry but only the literal `'updated'` for description (:210-213) — the description text stays out of the audit metadata. `downloadUrl` audits every issuance and returns a 300-second link (:238-249). |
| 52 | `apps/api/src/documents/inventory.service.spec.ts` | ts | 159 | READ | 159 lines, 4 describes, covering the pure human view — the SQL half is proved against real Postgres in `test/inventory.e2e-spec.ts`, because a fake DataSource would only assert the query string is the one the file already contains (:8-13). The empty corpus gets its own describe: it renders without an empty table, states outright that the assistant refuses every question in that state, and still prints an all-zero totals block (:64-87) — the case a report built against a populated database usually gets wrong. The finding the report exists for is pinned twice: the callout listing uncitable ACTIVE documents, and the row itself ending in `NO` so the table alone suffices (:117-147). A long title truncates rather than breaking column alignment, the generation time appears in the header only, and the absent `issuingBody`/`effectiveDate` are asserted to be *named* in the output rather than silently omitted (:150-158). |
| 53 | `apps/api/src/documents/inventory.service.ts` | ts | 366 | READ | 366 lines. `build()` (:123-278) runs one query with two LATERAL joins separating current-version chunks from superseded ones (:124-163), then derives `retrievable` from the same four filters `RetrievalService.search()` applies, mapped one-for-one in a block that names the coupling and says a governance report disagreeing with the engine it describes is worse than no report (:174-203). `issuingBody` and `effectiveDate` are always `null` and listed in `fieldsNotInSchema` (:87-98) because the `documents` table records neither, and inferring them from a title would be right often enough to be trusted and wrong often enough to mislead (:22-29). Deterministic by construction: no generation timestamp in the JSON, ordered by (title, id), so two runs diff cleanly (:31-36, :159-160). `renderInventoryTable()` (:297-366) is the pure human view; an empty corpus is stated outright as the state in which the assistant refuses everything (:308-314). |
| 54 | `apps/api/src/dose/dose.controller.ts` | ts | 95 | READ | Four routes, 95 lines. `CalculateDto` (:28-36) requires a UUID formula and a positive weight; `CreateFormulaDto` (:38-50). `GET /dose/formulas?all=true` is honoured only for callers who also hold `DOSE_FORMULAS_MANAGE`, checked in the handler rather than the guard (:68-71). Formula creation is PHI-screened under the METADATA profile (:74-77). |
| 55 | `apps/api/src/dose/dose.module.ts` | ts | 13 | READ | 13 lines. Registers `DoseFormula` and `DoseCalculation`, exports `DoseService`. |
| 56 | `apps/api/src/dose/dose.service.spec.ts` | ts | 108 | READ | 108 lines, 6 cases. An unapproved formula is refused outright (:46-51). The arithmetic is checked against stated numbers rather than recomputed by the test (15 mg/kg × 20 kg = 300 mg, 30 mL at 10 mg/mL), and `safetyWarning` is asserted twice — once against `DOSE_SAFETY_WARNING_AR` and once against the Arabic literal spelled out in the test — so a reworded constant fails here (:53-64). Single-dose and daily caps, the >10% deviation warning, and implausible weights (0 kg, 500 kg) are each pinned (:66-107). |
| 57 | `apps/api/src/dose/dose.service.ts` | ts | 224 | READ | Deterministic arithmetic, no model involved. `calculate()` bounds weight to 0-400 kg (:88-90) and **refuses an unapproved formula outright** (:94-99) — the safety gate. Three formula types (:112-153); a daily dose over `maxDailyDose` is capped with a warning rather than returned (:133-141), and the same again for `maxSingleDose` (:155-161). Two clinical warnings are generated: a prescribed dose differing by more than 10% (:171-178) and a patient under one year (:179-183). Every step is recorded as human-readable text and persisted with the calculation (:185-195). `DOSE_SAFETY_WARNING_AR` is attached verbatim to every result (:220-221). Note for the data-model report: `dose_calculations.inputs` stores weight and age — patient attributes, not identifiers. |
| 58 | `apps/api/src/entities/ai.entity.ts` | ts | 119 | READ | `AiQuestion` (:110) storing the question text and channel; `AiAnswer` (:40) with jsonb `steps`/`warnings`, `refused`, `latency_ms` and `review_status`, and eager cascading citations (:80); `Citation` (:88) which **denormalises** document title, page and approval date so a historical answer survives corpus changes. |
| 59 | `apps/api/src/entities/document.entity.ts` | ts | 158 | READ | Four entities: `Document` (:13), `DocumentVersion` (:66) cascading from the document, `DocumentChunk` (:104) whose `embedding` column is deliberately unmapped and handled in raw SQL (:100-103), and `DocumentApproval` (:132) with a nullable `actor` for system-driven transitions. |
| 60 | `apps/api/src/entities/dose.entity.ts` | ts | 110 | READ | A `numericTransformer` (:14-17) parses Postgres `numeric` back to a JS number, so dose arithmetic never operates on strings. `DoseFormula` (:19) holds the four `numeric(12,4)` dose limits, status and source document; `DoseCalculation` (:82) stores `inputs`, `steps` and `warnings` as jsonb alongside the computed dose. |
| 61 | `apps/api/src/entities/index.ts` | ts | 46 | READ | Barrel re-exporting all 15 entity classes and the `entities` array TypeORM is configured with (:30-46). |
| 62 | `apps/api/src/entities/misc.entity.ts` | ts | 86 | READ | `AuditLog` (:10) with actor id **and** actor email, action, jsonb metadata, ip and user agent; `Notification` (:43) with a nullable `user_id` for broadcasts; `Setting` (:70) keyed by a string primary column. |
| 63 | `apps/api/src/entities/user.entity.ts` | ts | 97 | READ | `PermissionEntity` (:11) with UNIQUE `code`, `Role` (:26) with UNIQUE `name` and an eager many-to-many over `role_permissions` (:37-43), and `User` (:49) carrying `token_version`, `failed_login_attempts`, `locked_until`, `mfa_enabled`/`mfa_secret`, and an eager many-to-many over `user_roles` (:84-90). |
| 64 | `apps/api/src/eval/field-eval.spec.ts` | ts | 304 | READ | 304 lines, 9 describes. Every invariant in `checkInvariants` is pinned from both sides — a well-formed answer and refusal pass, then each contract breach is caught individually: a paraphrased refusal, citations attached to a refusal, non-NONE confidence, an unnamed or unknown gate, `MODEL_ERROR` counted as governance, an uncited answer, a citation missing a title or approval date, an answer claiming to have refused, empty text, and the refusal string leaking into a cited answer. One case asserts the violation message **never republishes the offending text** — if a refusal ever carries model text, a report that prints it has published the thing the refusal existed to withhold (:61-66). `evaluateCase` is asserted to expose no field claiming the answer was correct (:149-150). The sheet cases pin that the four judgement columns stay empty, that it says a generated sheet is not a completed review, that it warns coverage is not a score, and that engineering-authored provenance is flagged in the header rather than buried. |
| 65 | `apps/api/src/eval/field-eval.ts` | ts | 422 | READ | 422 lines, read in three chunks. The scoring core, pure and taking an `ask` function so the same code runs in the e2e suite and against a live deployment (:21-23). The gated/measured line is stated at the top (:5-19). `checkInvariants` (:69-127) is the gate: five properties that hold on any corpus — a refusal returns `REFUSAL_MESSAGE_AR` verbatim with no citations and `NONE` confidence, an answer carries a citation with a title and an approval date, `refusedAt` names one of the four `KNOWN_GATES` (:62), `MODEL_ERROR` is explicitly **not** counted as governance because an outage reading as a correct refusal turns a broken provider into evidence of good governance (:98-103), and nothing 400s or 5xx's (:73-80). A refusal violation deliberately does not print the answer: if a refusal ever carries model text, the last thing a report should do is republish it (:84-86). `paraphraseAgreement` (:350-381) is the one clinically meaningful measurement needing no ground truth — two wordings should reach the same verdict and document, and disagreement means retrieval matched vocabulary rather than meaning (:330-340). `simulateThresholds` (:401-422) reconstructs the answer-vs-refuse trade-off from scores the run already returned, because the live runner cannot change a deployment's `RAG_MIN_SIMILARITY` and must not try (:383-393); it simulates the threshold gate only and the report prints that limit. `renderReviewSheet` (:205-328) emits the §5.2 sheet with four judgement columns left as empty boxes — "this produces the paperwork, not the verdict" (:198-204) — and prints the engineering-authored count as a warning at the top when there is one (:246-254). |
| 66 | `apps/api/src/eval/field-set.spec.ts` | ts | 176 | READ | 176 lines, 6 describes. Parser cases pin file-and-line error reporting, duplicate ids, required fields, unknown provenance (which cannot have a default because it decides what a result is worth, :42-45), and that an empty set is rejected because it measures nothing. One describe enforces the independence guarantee rather than documenting it: each forbidden key is rejected *and* the message is asserted to explain why (:66-86). PHI cases assert a question with an identifier is refused before it can be sent anywhere, that the matched pattern is named so the author can rewrite, and that an ordinary clinical question with numbers passes. The last describe is the strongest: it reads every non-spec module under `src/eval` and asserts none contains `sample-docs`, and reads the starter case file and asserts it names none of the four seeded document titles (:142-169) — turning the absence of the import that made the gold set circular into something that fails the build if it returns. |
| 67 | `apps/api/src/eval/field-set.ts` | ts | 231 | READ | 231 lines. Loader and validator for the non-circular evaluation set. The header states the circularity it exists to break: the gold set's questions were written by reading the four documents they retrieve from (:4-27). `FORBIDDEN_KEYS` (:94-99) rejects `expectSource`, `expectAnswerContains`, `expectedAnswer` and `expectRefusal` with a reason each — the moment a case file can name the document that should answer it, someone will fill it in by reading the corpus. `CaseProvenance` (:42) is required with no default because it decides how much a result is worth, and only `ward-submitted` makes a coverage figure mean anything (:31-41). `parseFieldSet` (:121-203) skips blank and `#` lines, names the file and line on every failure because case files are edited by people who do not read stack traces (:113-119), rejects duplicate ids and dangling `paraphraseOf` references, and runs every question through `scanForPhi` at load time so an identifier is caught from the operator's own file rather than from a 400 mid-run — and, for the HTTP runner, before the text leaves the machine (:168-179). |
| 68 | `apps/api/src/health.controller.spec.ts` | ts | 58 | READ | 58 lines, 5 cases. Liveness answers without touching a dependency (:17-20). Readiness is checked in all four dependency combinations, asserting both the HTTP status set on the mocked `res` and which dependency is named down (:22-57) — so a probe that reported 200 while Postgres was unreachable would fail here. |
| 69 | `apps/api/src/health.controller.ts` | ts | 63 | READ | Two public probes, 63 lines. `GET /health` is liveness — dependency-free by design so a degraded Postgres cannot make Kubernetes restart healthy pods in a loop (:10-15, :30-34). `GET /health/ready` checks the database and object storage in parallel and sets 200/503 on the response object rather than throwing (:36-53), deliberately bypassing `AllExceptionsFilter` because a 5xx there writes an `ERROR:UNHANDLED` row per probe poll and redacts the detail an operator needs (:16-21). `checkDatabase()` is `SELECT 1` in a try/catch (:55-62). |
| 70 | `apps/api/src/mail/mail.module.ts` | ts | 9 | READ | `@Global()` module providing and exporting MailService (:106-111). |
| 71 | `apps/api/src/mail/mail.service.spec.ts` | ts | 87 | READ | 87 lines, 2 describes. `sendQuietly` swallows provider failures so a caller cannot leak whether an address exists (:12-20). The second describe boots the module three times under `jest.isolateModulesAsync` with `NODE_ENV` set before the graph loads — because `isProduction` binds at env.ts load — and asserts a `MAIL:LOG_PROVIDER_IN_PRODUCTION` audit event is recorded when production boots on the log provider, and not recorded outside production or when smtp is properly configured (:28-86). |
| 72 | `apps/api/src/mail/mail.service.ts` | ts | 135 | READ | Two providers behind one service. `LogMailProvider` (:31-40) writes the message to the log instead of delivering it, mirroring the mock LLM so the reset flow works with zero configuration; the docblock states plainly that anyone with log access can read reset links (:22). SMTP transport is built **lazily** (:47-64) so a broken mail dependency cannot take down API boot. `onApplicationBootstrap` (:94-114) writes a permanent `MAIL:LOG_PROVIDER_IN_PRODUCTION` audit row, because a console warning scrolls away and the audit trail is where a knowledge manager already looks. `sendQuietly` (:120-134) swallows and logs, so a relay failure cannot turn response time into an account-enumeration oracle. |
| 73 | `apps/api/src/main.ts` | ts | 48 | READ | Bootstrap, 48 lines. `loadEnv()` first so the secret fail-fast runs before a port is bound (:13-15). Nest's body parser is disabled and `express.json`/`urlencoded` re-added at `env.bodyLimit` so the JSON cap is controlled here while multer handles multipart (:17-28). `helmet()` (:26), explicit CORS allowlist where an empty list becomes `origin: false` rather than open (:30-35), `ValidationPipe({whitelist, transform})` (:37), `AllExceptionsFilter` wired to `AuditService` (:38), shutdown hooks (:40), and a boot log naming env and origins (:42-45). |
| 74 | `apps/api/src/migrations/1720000000000-initial-schema.ts` | ts | 258 | READ | Creates the `vector` extension (:9) and all 17 tables (:11-232), plus six indexes including the HNSW cosine index on `document_chunks.embedding` (:103-105). Carries its own `const EMBEDDING_DIM = 384` (:3), which is a different constant from the one `embedding.service.ts` reads from the environment — the reason `embeddingColumnDimension()` exists. `down()` drops all 17 tables in dependency order (:233-257). |
| 75 | `apps/api/src/migrations/1720000001000-token-version.ts` | ts | 21 | READ | Adds `users.token_version int NOT NULL DEFAULT 0` (:13-15). The docblock states the purpose: incrementing it on logout or password change invalidates every outstanding refresh token, turning stateless JWTs into revocable credentials (:3-8). |
| 76 | `apps/api/src/migrations/1720000002000-account-security.ts` | ts | 25 | READ | Adds `failed_login_attempts` and `locked_until` (:33-39). The docblock notes the per-account gate complements the per-IP limiter, which an attacker can rotate around (:24-29). |
| 77 | `apps/api/src/migrations/1720000003000-embedding-provider.ts` | ts | 35 | READ | Adds `document_chunks.embedding_provider` defaulting to the mock provider's name and indexes it (:63-72). The default backfills correctly: every chunk predating this migration was mock-embedded (:57-58). |
| 78 | `apps/api/src/migrations/1720000004000-chunk-uniqueness.ts` | ts | 53 | READ | Collapses existing duplicates keeping the newest of each group (:33-40), then adds UNIQUE `(document_id, version_number, chunk_index)` (:41-45). The docblock explains why the advisory lock alone is not enough: the constraint also covers a second API instance, which an in-process mutex would not, and notes that `citations.chunk_id` being ON DELETE SET NULL is what lets the de-duplication run without destroying answer history (:24-27). |
| 79 | `apps/api/src/notifications/notifications.controller.ts` | ts | 28 | READ | Two routes, both `@Permissions(NOTIFICATIONS_READ)`: `GET /notifications` delegating to `listForUser(user.userId)` (:14-18) and `POST /notifications/:id/read` with `ParseUUIDPipe`, scoped by the caller's id (:20-27). |
| 80 | `apps/api/src/notifications/notifications.module.ts` | ts | 17 | READ | 17 lines. Registers `Notification`, `Document` and `User`, and imports `DocumentsModule` for `ApprovalService`, which owns every lifecycle transition including the expiry the cron drives. The comment at :9-11 records that the dependency is one-way — `DocumentsModule` does not import this module — which is what keeps it from being a circular import. |
| 81 | `apps/api/src/notifications/notifications.service.spec.ts` | ts | 308 | READ | 308 lines, 3 describes, with hand-modelled repository fakes that actually filter rather than echo canned rows — because the behaviour under test *is* which documents get a notification (:9-12). The header is explicit about what the fakes cannot prove (that `metadata ->> 'documentId'` is valid Postgres) and names the e2e case that covers it (:12-15). The central case is a regression: de-duplication used to read the newest `DOCUMENT_NEAR_EXPIRY` row across the whole table and compare its documentId to the document in hand, which works with one near-expiry document and cannot with two — each sweep, whichever document did not raise the latest row is warned again, so A and B alternate re-notifying and emailing every manager daily forever, filling the governance surface with duplicates until it stops being read (:228-253). Also pins that expiry is delegated to the state machine rather than assigned in place, that the three governance roles are notified and nobody else, and that the in-app row is written independently of the email. |
| 82 | `apps/api/src/notifications/notifications.service.ts` | ts | 154 | READ | `listForUser` returns the caller's rows plus unaddressed ones via `IsNull()` (fixed in 93d82b1); `markRead` scopes by `{ id, userId }`. The daily cron at `EVERY_DAY_AT_6AM` (:39) hard-expires overdue ACTIVE documents through the approval state machine, then warns on documents within 30 days of expiry with a per-document de-duplication query, notifying and emailing every knowledge manager, hospital admin and super admin. |
| 83 | `apps/api/src/rag/chunking.service.spec.ts` | ts | 55 | READ | 55 lines, 4 cases. Chunks stay inside page boundaries so citations are page-accurate, with globally sequential indexes (:6-21). No chunk starts mid-word — tested with a drug monograph whose multi-syllable names a blind character slice would bisect, asserting each chunk's opening token exists in the source ("fonicid" from "Cefonicid" would fail, :23-40). Overlap is carried rather than dropped (:42-49), and near-empty fragments are discarded (:51-54). |
| 84 | `apps/api/src/rag/chunking.service.ts` | ts | 69 | READ | Page-aware chunking: chunks never cross a page boundary, so every chunk cites exactly one page (:20-30). Splits on sentence boundaries around 800 chars with 150 chars of overlap (:10-11, :32-46). `overlapTail` (:59-68) resumes at a sentence or word boundary and carries nothing rather than a fragment — the docblock records why in a drug manual: a mid-word cut turns 'Cefonicid sodium' into 'fonicid sodium', which embeds as a different, non-existent drug. |
| 85 | `apps/api/src/rag/embedding.service.spec.ts` | ts | 126 | READ | 126 lines, 3 describes. The mock provider is asserted deterministic, 384-dimensional and unit-normalised, and to rank lexically related text above unrelated (:6-30). `batchInputs` is tested on the count ceiling, the character budget, and the case that matters clinically — an oversized single input gets its own request rather than being dropped, because truncating a chunk would silently change what the assistant can cite (:55-60) — plus order preservation over 250 inputs and the empty case. The OpenAI provider describe pins the failure it was written for: a whole PDF embedded in one oversized request that the provider answered 400 to, an ingestion failure that got *more* likely the bigger the document was (:85-90), and a wrong embedding count fails loudly rather than misaligning every chunk with its vector (:112-125). |
| 86 | `apps/api/src/rag/embedding.service.ts` | ts | 186 | READ | Two providers behind one service. `tokenize` (:39-45) strips punctuation and a bilingual stopword list (:26-37) whose rationale is stated: function words dominate naive overlap and could push an off-topic question past the refusal threshold. `MockEmbeddingProvider` (:53-74) is a deterministic FNV-1a hashed bag-of-words with bigrams and two projections per term. `batchInputs` (:108-131) splits by count and characters, giving an oversized single input its own request rather than truncating a clinical chunk (:101-107). `OpenAiEmbeddingProvider.embed` (:137-158) runs batches **sequentially** to stay inside rate limits during a full reindex, pins `dimensions: EMBEDDING_DIM` and throws on a count mismatch. Selection at :167-171 is the fall-back this audit made fail-fast in config/env.ts. |
| 87 | `apps/api/src/rag/indexing.service.ts` | ts | 369 | READ | 369 lines, read in full. `onApplicationBootstrap` (:67-124) always logs the index state — provider, chunk total, staleRetrievable, staleOrphaned, column width and the live `refusalThreshold` via `ragMinSimilarity()` (:92-97) — and warns only on `staleRetrievable` (:102-112), because warning on the total made one expired document a permanent alarm. `providerCoverage` (:142-183) mirrors retrieval's governance filters exactly (:157-169) and derives `staleOrphaned` by subtraction so a chunk whose document row is gone is counted rather than dropped by the join (:176-179). `embeddingColumnDimension` (:198-214) reads `pg_attribute.atttypmod` — the only source that can answer, since the migration's `EMBEDDING_DIM` constant and the one `embedding.service.ts` reads from the environment are different constants (:185-197). `indexDocument` (:217-272) takes `pg_advisory_xact_lock(hashtextextended(id))` inside the transaction (:242-244) before DELETE-then-INSERT; the comment records the READ COMMITTED race it prevents and names migration 1720000004000's UNIQUE constraint as the backstop. `reindexStale` (:298-314) narrows the work to documents the assistant currently cannot see; `reindexDocument` (:327-340) is the infrastructure route that re-embeds one live document without writing approval history. |
| 88 | `apps/api/src/rag/llm.service.ts` | ts | 165 | READ | Two providers. `LlmAnswer.failed` (:10-17) exists so a provider outage and a governed 'the context does not answer this' are distinguishable — the docblock calls conflating them the most misleading failure the system can produce, and `RagQueryService` maps it to MODEL_ERROR. `MockLlmProvider` (:37-77) is extractive: it splits retrieved chunks into lines, scores them by query-token overlap normalised by length (:52-58), and composes shortAnswer from the top three verbatim lines — it has no generative capability, so it cannot hallucinate. `OpenAiLlmProvider` (:84-145) sends a context-only system prompt (:102-114) with `temperature: 0` and `response_format: json_object`, instructing the model to reply in the question's language (:105-108) and to keep shortAnswer non-empty whenever the excerpts do answer (:109-113), because procedural questions otherwise leave it empty and the caller reads that as 'nothing found'. Both an unexpected response shape (:123-129) and any throw (:137-143) return `failed: true` rather than an empty answer. |
| 89 | `apps/api/src/rag/openai-http.spec.ts` | ts | 129 | READ | 129 lines, 2 describes. `openAiPost` is asserted to log the upstream error body rather than discard it, to treat 400 as non-retryable (fetch called once) and 5xx as retryable (called twice), and not to throw when the failure body cannot be read (:18-79). The `redactSecrets` describe tests the control in both directions: three key shapes are stripped, and — the half that is easy to omit — five real provider diagnostics (`'$.input' is invalid`, `invalid_api_key`, the quota message, an unknown-model message, `Invalid dimensions`) must survive intact, because the redactor exists to protect a diagnostic and must not eat one (:108-121). A body with nothing key-shaped in it is asserted byte-for-byte unchanged (:123-128). |
| 90 | `apps/api/src/rag/openai-http.ts` | ts | 84 | READ | One shared POST helper with a hard timeout (`AbortSignal.timeout`, :60) and exactly two attempts, retrying only on 429/500/502/503/504 or a network error (:5, :51, :73). The error body is logged because it holds the only clue to why a request was rejected (:64-72), passed through `redactSecrets` (:32-37) whose three patterns (:19-30) are deliberately narrow — the comment notes a redactor that swallowed `invalid_api_key` would undo the logging it protects. Final failure throws (:81-83). |
| 91 | `apps/api/src/rag/pdf-extraction.service.spec.ts` | ts | 99 | READ | 99 lines, 5 cases, fixtures built by the seeder's own `buildPdf` so these are real PDF bytes with a real text layer. The header corrects the claim that extraction was untestable, and names the actual mechanism — pdf-parse choked on a `Buffer` rather than a plain `Uint8Array`, and because the damage depends on Node's shared buffer pool the same file parsed on one run and threw on the next, which is what made the bug look environmental (:4-19). The regression pin repeats extraction ten times on a deliberately sub-4096-byte document, and asserts that size, so the test cannot silently stop covering the bug if pdfkit output grows (:55-75). Also covers per-page confinement, whitespace collapsing, and the scanned-PDF case with no text layer (:89-98). |
| 92 | `apps/api/src/rag/pdf-extraction.service.ts` | ts | 65 | READ | Per-page text extraction via pdf-parse, with a `pagerender` hook that inserts a newline on each Y-coordinate change (:115-127). Hands pdf.js a plain `Uint8Array`, never a `Buffer` (:113) — the 30-line docblock (:80-110) records the measured cause: pdf.js clones with `new value.constructor(value)`, a Buffer clone allocates from Node's shared 8 KB pool at a non-zero byteOffset, and pdf.js then misreads it. Measured 12/12 parses with Uint8Array against 8/12 with Buffer. |
| 93 | `apps/api/src/rag/provider-consistency.spec.ts` | ts | 207 | READ | 207 lines, 3 describes. Both halves of the provider-consistency contract are pinned against the SQL actually executed: `INSERT INTO document_chunks` carries `embedding_provider` with the active provider's name, and the retrieval query contains `c.embedding_provider = $3` with that name bound — including when a category filter is also applied, which is the variant a refactor would drop (:80-94). The boot-summary describe covers the operator-visibility case: the summary is logged *even when nothing is stale*, because on a deployment whose HTTP surface is unreachable this line is the only way to read the index state, and absence of a warning is not evidence (:123-145). Three cases pin the threshold reporting: the configured value, the effective default when the variable is unset — hosting platforms redact variable *values*, so an operator can see `RAG_MIN_SIMILARITY` is set without seeing what to (:165-189) — and an unreadable column width reported as `unknown` rather than as a number. |
| 94 | `apps/api/src/rag/rag-provider-check.spec.ts` | ts | 233 | READ | 233 lines, 8 cases. Pins the properties that make the endpoint safe to hand an operator: it reports rather than throws (throwing would route through `AllExceptionsFilter`, which in production replaces the body with a generic message — destroying the one thing the operator opened it to read, :76-79), it redacts a credential that surfaced in the error, and it still reports corpus coverage when the probe fails, which is what separates "dead provider" from "corpus embedded by a different one" (:120-143). The last three cases pin a bug the endpoint shipped with: the column width is fixed by the initial migration's own `const EMBEDDING_DIM = 384`, a *different* constant from the `process.env.EMBEDDING_DIM` the service reads, so configuring 1536 with a genuine 1536-dimension provider reported `ok: true` while every INSERT failed — the check compared the configuration against itself (:145-160). The test's own header records that a first version used 384 on both sides and passed against the unfixed code, so it rebuilds the module with a differing value and asserts the premise (`expect(fresh).toBe(CONFIGURED)`) before asserting the behaviour (:166-172). |
| 95 | `apps/api/src/rag/rag-query.service.spec.ts` | ts | 199 | READ | 199 lines, 4 describes. The refusal contract is asserted against both the constant and the literal Arabic string, so a reworded constant fails here (:36-47). All four gates are pinned by name: `NO_CANDIDATES`, `BELOW_THRESHOLD`, `MODEL_FOUND_NOTHING`, and `MODEL_ERROR` — the last separating a provider outage from a corpus gap, which is the distinction operators need because the library may well cover the question (:121-127). A steps-only and a warnings-only answer both count as answers (:101-112). One case is a cross-language regression: an Arabic question over an English chunk has zero token overlap, and the old rerank formula collapsed a 0.3 similarity to 0.18 and refused — coverage must stay a bonus, never a penalty (:72-81). The diagnostics describe pins that the retrieved pool and what actually reached the model are reported separately, since reporting the pool size as the qualifying count would tell a manager the model saw evidence it never received (:143-155). |
| 96 | `apps/api/src/rag/rag-query.service.ts` | ts | 209 | READ | The governed chain. `RagDiagnostics` (:24-61) carries candidateCount, qualifiedCount, bestScore, the effective threshold, the top three considered snippets and which gate refused. One private `refusal()` (:84-97) is the single exit for all four gates — NO_CANDIDATES (:122-131), BELOW_THRESHOLD (:160), MODEL_ERROR (:167-172) and MODEL_FOUND_NOTHING (:181-183) — so every refusal returns `REFUSAL_MESSAGE_AR` verbatim with zero citations and NONE confidence by construction, not by four separate remembered decisions. `ask()` re-reads the validated threshold per call (:117). Candidates are scored *before* thresholding so the diagnostic reports the true best score even when nothing qualifies (:133-138). `hasContent` accepts steps or warnings, not just shortAnswer (:177-180), because procedural answers put the substance in steps. Confidence bands at 0.6/0.45 (:99-103); citations carry document, page, approval date, similarity and a 300-char snippet (:198-208). |
| 97 | `apps/api/src/rag/rag.controller.ts` | ts | 264 | READ | Six routes, all behind explicit permissions. `POST /rag/provider-check` (:71-149) exercises the embeddings provider with one throwaway probe, compares the returned width against `embeddingColumnDimension()` rather than `EMBEDDING_DIM` (:81-82) — the docblock records that checking EMBEDDING_DIM against EMBEDDING_DIM was the configuration describing itself — reports `dimensionConfigMismatch` separately (:108-113), returns corpus coverage even when the probe fails (:116-118), and never returns the provider's raw body (:60-63). It is deliberately not part of `/health/ready` (:56-59). `POST /rag/reindex`, `/reindex/stale` and `/reindex/:documentId` (:157-228) each audit with the provider and outcome counts; the single-document route is on `/rag` rather than `/documents` because the documents route is an approval transition that refuses an ACTIVE document (:198-207). `POST /rag/query` is screened for PHI even though it persists nothing, because the text reaches the LLM provider and sent text cannot be recalled (:233-236). |
| 98 | `apps/api/src/rag/rag.module.ts` | ts | 36 | READ | Registers RagController and the eight RAG providers (:146-157). Exports IndexingService, RagQueryService, RetrievalService, RerankService and EmbeddingService, the last for its `name` only so the inventory report reads the active provider from the same object retrieval filters on (:158-168). |
| 99 | `apps/api/src/rag/rerank.service.spec.ts` | ts | 81 | READ | 81 lines, 2 describes. Source diversity is tested against the live failure it was written for — a compatibility manual out-scoring a dilution manual on every chunk because it names the drug constantly — asserting the second document still reaches the shortlist and the first is capped at three (:34-44). The complement matters as much: when only one document has anything to offer the shortlist is still filled, because a hard cap would starve the model of evidence (:46-50). Scoring cases assert a chunk is never demoted below its semantic similarity (an Arabic query over English text has zero lexical overlap, :62-67) while overlap still acts as a promotion bonus (:69-80). |
| 100 | `apps/api/src/rag/rerank.service.ts` | ts | 91 | READ | Lexical reranker standing in for a cross-encoder. Score is `max(similarity, 0.6·similarity + 0.4·coverage)` (:148-151) — promotion-only, so an Arabic question over an English source (coverage 0) never faces a harsher effective threshold than a same-language one. `selectDiverse` (:177-209) caps chunks per document at `ragMaxPerDocument()` with backfill so the cap never returns fewer chunks than plain ranking; the docblock records the live failure that motivated it (:159-176). |
| 101 | `apps/api/src/rag/retrieval-dimension-mismatch.spec.ts` | ts | 67 | READ | 67 lines, 3 describes. A pgvector `different vector dimensions 384 and 1536` is absorbed into an empty result so the question routes through `NO_CANDIDATES` and the nurse gets the governed refusal instead of a generic 500 (:33-39). The log is asserted to name the provider's width and point at `provider-check` (:41-55). The third case is the guard on the narrowness: three unrelated database errors — connection terminated, missing relation, statement timeout — must still raise (:57-66), because a refusal that hides a broken database would be worse than an error (:15-16). |
| 102 | `apps/api/src/rag/retrieval.service.ts` | ts | 118 | READ | Vector search over pgvector. One parameterised query (:60-79) applying the four governance filters — `d.status = ACTIVE` (:71), unexpired (:72), `c.version_number = d.version_number` (:73), `c.embedding_provider = <active>` (:74) — ordered by cosine distance with `LIMIT topK` (:76-77). The catch at :80-104 is deliberately narrow: only `different vector dimensions` is swallowed into zero candidates so the question routes to the governed refusal, and every other database error re-raises (:95). `topK` now comes from the validated `ragTopK()` (:43). |
| 103 | `apps/api/src/roles/roles.controller.ts` | ts | 36 | READ | A single `GET /roles` behind `ROLES_READ` (:120-124). The docblock (:95-115) records why POST /roles and PATCH /roles/:id were removed: they wrote to `role_permissions`, returned success and emitted an audit entry while changing nothing a user could do, because authorization reads the matrix in `rbac.ts`. |
| 104 | `apps/api/src/roles/roles.module.ts` | ts | 13 | READ | Registers the Role entity, RolesController and RolesService; exports RolesService (:132-137). |
| 105 | `apps/api/src/roles/roles.service.ts` | ts | 26 | READ | One method, `findAll()`, returning the seeded role catalogue with permission codes for display (:80-88). Read-only by design. |
| 106 | `apps/api/src/scripts/create-admin.spec.ts` | ts | 38 | READ | 38 lines, 4 describes. Pins the break-glass password policy, and the header states why it matters: that credential exists *because* the demo passwords were weak and public, so accepting a weak one would reintroduce the hole it closes (:4-8). Table-driven rejection of short/no-uppercase/no-lowercase/no-digit/no-symbol (:14-22), a case generated from every entry in `DEMO_ACCOUNTS` asserting the published literals are refused (:26-31), and one asserting all failed rules are reported at once rather than one per attempt (:33-37). |
| 107 | `apps/api/src/scripts/create-admin.ts` | ts | 171 | READ | 171 lines. Break-glass provisioning, and the counterpart to `DemoAccountGuardService`: on a deployment that was only ever seeded the guard disables all seven accounts, so without this the fix for the credential exposure would lock the hospital out of its own platform (:12-15). `assertStrongPassword` (:42-59) requires 12 characters with mixed case, a digit and a symbol, and explicitly rejects any password in `DEMO_ACCOUNTS` (:51-55). `provisionAdmin` (:69-151) creates the SUPER_ADMIN role with its permission rows if absent (:86-104), then is **idempotent when nothing needs doing** — same password, active, role attached — because the container runs it at every boot and an unconditional reset would bump `token_version` each deploy, revoking the administrator's sessions as a side effect of releasing unrelated code (:108-124). The reset path clears `failedLoginAttempts`/`lockedUntil` and bumps `tokenVersion` (:127-143). The password is never echoed, because this output goes to the deployment log (:139-141). |
| 108 | `apps/api/src/scripts/field-eval.ts` | ts | 242 | READ | 242 lines. Runs a field set against a live deployment over plain `fetch` — no Nest context, no database — so it works from a laptop against a container with no shell (:23-25). Four deliberate properties, each with its reason in the header (:27-43) and enforced in code: the password comes from `EVAL_PASSWORD` and never from an argument because arguments reach `ps` and shell history (:66, :132-133); `loadFieldSet` screens every question locally so a case carrying a real identifier never leaves the machine (:135-137); the run **refuses** unless the account's roles include `NURSE_USER`, since a manager sees what a nurse cannot and would measure a different product (:151-157); and it changes nothing on the target — `POST /rag/query` persists no answer and the threshold sweep is simulated (:37-39). Handles MFA (:107-124), re-logs in on a 401 mid-run rather than losing everything asked so far (:170-175), and backs off on 429 (:176-179). Refuses to run unattended without `--yes` (:146-148). `--json` output carries no timestamp so runs diff cleanly (:202-205). |
| 109 | `apps/api/src/scripts/inventory.ts` | ts | 53 | READ | 53 lines. CLI front-end for the same report `GET /documents/inventory` serves. Boots an application *context* rather than an HTTP server, so it composes the real `InventoryService` with the real `EmbeddingService` and reports exactly what the route would (:20-22). `--json` suppresses all Nest logging because boot chatter on stdout would corrupt the JSON (:31-33), and only the report goes to stdout so `--json > report.json` diffs cleanly against yesterday's (:24-25). States plainly that it cannot be run against the live deployment — the API container ships without a shell (:15-18). |
| 110 | `apps/api/src/scripts/migrate.ts` | ts | 17 | READ | 17 lines. Initialises `AppDataSource`, runs pending migrations, prints the names applied or `Database schema is up to date.`, destroys the connection. Exits 1 on failure (:14-17). This is the `node apps/api/dist/scripts/migrate.js` entry the container runs before `main.js` — which is why `data-source.ts` is the earliest code to touch production secrets. |
| 111 | `apps/api/src/seed/demo-accounts.ts` | ts | 54 | READ | 54 lines. The seven demo accounts and their shipped passwords, in one place because two consumers need the same list for opposite reasons: `seed.ts` creates the accounts from it, and `demo-account-guard.service.ts` uses it in production to detect and disable an account still carrying one (:10-14). The literals are treated as **known-compromised** rather than as secrets, since they are published in README and in this public repository (:7-9). `seedPasswordFor()` (:52-54) applies a `SEED_PASSWORD_<ROLE>` override, and the docblock states that the guard must never call it: an operator who set an override chose a real secret, so comparing against the override would disable a properly-provisioned account (:16-20, :45-51). |
| 112 | `apps/api/src/seed/pdf.ts` | ts | 37 | READ | 37 lines. `buildPdf(title, pages)` renders page-arrays of paragraphs into an in-memory pdfkit buffer. `compress: false` (:16) is no longer load-bearing and the comment says so: the original note blamed pdf.js choking on compressed object streams, but the real cause was compression shrinking files below Node's 4096-byte Buffer pooling threshold and pdf.js ignoring `byteOffset` on a pooled buffer — fixed at the extraction side, so the flag stays only because uncompressed output is easier to inspect (:6-15). |
| 113 | `apps/api/src/seed/refuse-in-production.ts` | ts | 17 | READ | 17 lines, imported for its side effect and imported **first** by `seed.ts`. The docblock records why it is a module of its own: the policy check must run before `AppModule` loads, because loading that pulls in `config/env.ts` whose secret fail-fast throws first — so an operator who asked to seed production was told `JWT_SECRET must be set to a non-default value`, would supply the secrets, run it again, and seed seven published-password accounts into a live clinical system (:6-12). |
| 114 | `apps/api/src/seed/sample-docs.ts` | ts | 116 | READ | 116 lines, four demo documents marked "Not for real clinical use" (:11-13). IV Paracetamol preparation across two pages including the weight band that the paediatric dose formula cites (:16-47), a Hand Hygiene and Medication Administration Safety Policy over two pages (:48-73), a one-page CBAHI Medication Management MM-7 summary (:74-91), and a one-page Peripheral IV Cannulation Procedure (:92-115). This is the file the gold set's circularity is measured against: its questions were written by reading these four documents, which is why `eval/field-set.starter.jsonl` exists. |
| 115 | `apps/api/src/seed/seed-policy.spec.ts` | ts | 78 | READ | 78 lines, 2 describes. The first reloads the policy module with `isProduction` stubbed both ways and asserts the production refusal, that the refusal names `create-admin` as the alternative, that development and test seed normally, that the opt-in warns loudly, and that any value other than the exact string `true` is not an opt-in (:25-53). The second parses `seed.ts`'s own import list and asserts `./refuse-in-production` precedes `../app.module` and that only `reflect-metadata` comes before it (:63-77) — pinning an ordering that is load-bearing, invisible in the source, and that an import sorter would happily break (:56-62). |
| 116 | `apps/api/src/seed/seed-policy.ts` | ts | 30 | READ | 30 lines. `assertSeedingAllowed()` throws under `NODE_ENV=production` unless `SEED_ALLOW_PRODUCTION=true`, which warns loudly instead (:14-29). The docblock names this the *second* gate and why one was not enough: the container gate is a raw shell compare on `SEED_ON_BOOT` that never consults `NODE_ENV`, and the compose default is `true`, so the only thing between the shipped image and seven published-password accounts was an operator remembering a flag (:6-12). |
| 117 | `apps/api/src/seed/seed.ts` | ts | 267 | READ | 267 lines. `./refuse-in-production` is imported above every other import and a comment says it must stay there (:2-5). Repositories come from the `DataSource`, not Nest's per-module repository tokens, and the comment records what that cost: `app.get(getRepositoryToken(X))` only resolves while some module still lists X in a `forFeature([...])`, so making the roles API read-only dropped `PermissionEntity` from `RolesModule` and broke seeding — and because the container CMD tolerates a failed seed, the stack came up with no users at all (:79-86). The skip-if-present check keys on *any* demo email, because keying on one meant deleting that user re-ran the whole seed, hit the unique-email constraint on the remaining six and left the database half-applied (:98-109). Documents go through the real pipeline — upload, submit-review, approve, index — with CBAHI routed to the quality officer and everything else to the pharmacist (:159-192). Three dose formulas, one deliberately left DRAFT to demonstrate the safety gate (:234-248). The final summary prints emails and roles only: printing passwords put every credential into the deployment log, and if an operator had set `SEED_PASSWORD_<ROLE>` to a real secret it was *that* which got printed, so the documented mitigation leaked itself (:250-256). |
| 118 | `apps/api/src/settings/settings.module.ts` | ts | 81 | READ | Service, controller and module in one file, 81 lines. `SettingsService.upsert` (:32-51) preserves the existing description, stamps `updatedById`, and audits `SETTINGS:UPDATE` with a from/to metadata pair. `SettingsController` exposes `GET /settings` (`SETTINGS_READ`) and `PUT /settings/:key` (`SETTINGS_MANAGE`). The `PUT` body is typed `{ value: unknown }` (:68) rather than a validated DTO and `key` carries no pipe (:67) — recorded in 05-API-SURFACE.md as a reviewer item. |
| 119 | `apps/api/src/storage/storage.module.ts` | ts | 9 | READ | `@Global()` module providing and exporting StorageService (:97-102). |
| 120 | `apps/api/src/storage/storage.service.ts` | ts | 93 | READ | S3 client built entirely from `loadEnv().s3` (:28-35), with the comment recording that an inline `?? 'bnp_minio_secret'` here meant the object store silently used shipped demo credentials whenever the variable was absent. `isHealthy()` probes with HeadBucket and **deliberately does not create** the bucket (:38-51) — a self-healing readiness probe would hide a broken deployment. `ensureBucket()` creates on miss and swallows only `BucketAlreadyOwnedByYou` (:53-64). `presignedDownloadUrl` defaults to 300 seconds (:86-92). |
| 121 | `apps/api/src/users/users.controller.ts` | ts | 83 | READ | Five routes, 83 lines. `CreateUserDto` (:28-33) and `UpdateUserDto` (:35-40). `GET /users/me` is the only route here without `@Permissions` — it merges the DB-backed profile with the JWT-derived permissions, because permissions live in `rbac.ts` and not the database (:52-57). The other four are `USERS_READ` / `USERS_MANAGE`, with `ParseUUIDPipe` on every `:id`. |
| 122 | `apps/api/src/users/users.module.ts` | ts | 13 | READ | Registers User and Role entities, UsersController and UsersService; exports UsersService (:145-150). |
| 123 | `apps/api/src/users/users.service.ts` | ts | 153 | READ | 153 lines. `toDto` (:35-46) never emits `passwordHash`. `findAll` relies on the `roles` relation being `eager: true` on the entity (`user.entity.ts:84`). `me()` reads from the database rather than echoing the JWT so mid-session state — MFA enrolment above all — is visible before the next sign-in (:53-63). `resolveRoles` 400s if any name is unknown (:65-71). `create` lowercases the email, rejects duplicates, bcrypt cost 10 (:73-96). `update` builds a from/to `changes` map for the audit record and bumps `tokenVersion` on a password change, revoking outstanding refresh tokens (:98-135). `remove` is a soft delete — `isActive = false` so audit history stays intact (:137-152). |
| 124 | `apps/api/test/answer-quality.e2e-spec.ts` | ts | 331 | READ | 331 lines. The gold set through the whole governed chain. The gated/measured split is the design and is stated twice — in the header (:42-57) and in the generated report itself: **routing** is gated in CI, while whether the extract surfaced the specific figure is measured only, because under the mock LLM that scores a stand-in's sentence-picking heuristic rather than anything that ships (:116-123). Refusal cases assert `refusedAt === 'BELOW_THRESHOLD'`, and a `MODEL_ERROR` is explicitly scored as a routing failure so a provider outage cannot look like governance (:105-110). Assertions are on `routingFailure` — a string naming which document was reached — rather than a boolean, so a regression is diagnosed rather than merely detected (:149-151). `writeReport` (:188-331) is deliberately not an assertion: it emits a report whose first section is headed "What this is not", states that the mock embedder measures lexical retrieval and that four documents is an easier problem than four hundred, runs a `RAG_MIN_SIMILARITY` sweep from 0.15 to 0.4, and closes by telling the clinical reviewer that these questions were written from the corpus and the set that matters is one a nurse educator writes from practice. |
| 125 | `apps/api/test/auth.e2e-spec.ts` | ts | 298 | READ | 298 lines, 2 describes. Over real HTTP: a refresh token presented as an access token is rejected because `JwtStrategy` checks `payload.type` (:49-57); logout bumps `token_version` and **both** the pre- and post-refresh tokens are then asserted to 401, so revocation is proved to reach every outstanding token rather than the most recent (:59-86); lockout blocks a correct password; the `ValidationPipe` turns a malformed email into a 400 rather than a 500. The reset case walks the whole path — response body asserted to be exactly `{ requested: true }`, the link read out of the captured mail, the new password proved to work, the old one proved dead, and the token proved single-use on replay (:114-156). An unknown address returns the same body and mails nothing. One case asserts `POST /auth/register` answers **404** — the absence of public self-registration pinned as a test rather than a comment (:171-177). The MFA describe drives enroll → login-not-yet-challenged → wrong code rejected → enable → login challenged with no accessToken → verify → disable-needs-password → disabled, in one ordered case (:216-297). |
| 126 | `apps/api/test/create-admin.e2e-spec.ts` | ts | 102 | READ | 102 lines, 4 cases, against a real database because the role/permission bootstrap and the eager roles relation are exactly what a mocked repository would fake away (:20-22). Creates a SUPER_ADMIN on an empty database including the role rows; asserts the second boot is `ALREADY_PROVISIONED` with **the same tokenVersion and the same passwordHash** — the load-bearing assertion, since the container runs this at every boot and a re-hash would revoke the administrator's sessions on each unrelated deploy (:56-70). The third case reconstructs what `DemoAccountGuardService` leaves behind (`isActive: false`, a bumped tokenVersion) and asserts the rescue reactivates, attaches SUPER_ADMIN, sets the new password, invalidates the old one, and still leaves pre-rescue tokens revoked (:72-95). The fourth asserts the published demo password is refused even here. |
| 127 | `apps/api/test/document-lifecycle.e2e-spec.ts` | ts | 355 | READ | 355 lines. Nothing mocked but object storage: a real PDF is parsed, chunked, embedded, written to pgvector and retrieved by a real vector query (:33-37). The order of the cases is the argument — the same on-topic question is asked **before** approval and refused with the exact Arabic string and zero citations (:109-122), then after index → ACTIVE it is answered with a citation carrying title, page and approval date (:198-216), then after deactivate it is refused again and `document_chunks` is asserted empty (:252-275). Also pins that a nurse cannot drive the workflow (403 on approve, index and download-url), that indexing an unapproved document is a 400, that a second submit from IN_REVIEW is an illegal transition, and that chunks carry `mock-hash-embedding`. Two cron cases: expiry lands in the **approval history** attributed to `actor: null`, which matters because NURSING_KNOWLEDGE_MANAGER holds `documents:read` but not `audit:read` and so could not otherwise see anywhere in the product that a document had left retrieval (:277-310); and two near-expiry documents are warned once and not again on the next sweep — the case a fake cannot prove, since the de-duplication query reaches into jsonb with `->>` and either runs or throws only against the real engine (:312-354). |
| 128 | `apps/api/test/field-set.e2e-spec.ts` | ts | 146 | READ | 146 lines. Runs the starter field set through the real governed chain as a NURSE_USER. The header is the clearest statement of the WI-2 boundary in the repository: it never asserts which document a question should reach, because these questions were not written from the corpus and inventing an expected document would mean reading the corpus to write it — the exact circularity WI-2 exists to break (:38-43). What it gates is the five governed invariants, which hold on any corpus and are therefore the only things a build can honestly fail on when the corpus is a library nobody in CI has seen (:45-49). It also states why using the seeded corpus is not a contradiction: independence is a property of the questions, not of the corpus, so most cases will be refused against four demo documents and that is correct behaviour, which is why the refusal rate is reported and asserted nowhere (:51-56). Two guard cases: an author's expectation is gated only when the case names this corpus — gating nothing today and everything the day someone records one, with nobody having to remember to wire it up (:127-137) — and a final case asserting every case in the file actually ran, because a case that never ran reads exactly like a case that passed (:139-145). |
| 129 | `apps/api/test/health.e2e-spec.ts` | ts | 28 | READ | 28 lines, 2 cases. Liveness is asserted public and dependency-free; readiness is asserted to report `database: ok` and `objectStorage: ok` against the real stack, matching the exact body shape. The smallest e2e file, and the one that proves the harness itself boots. |
| 130 | `apps/api/test/inventory.e2e-spec.ts` | ts | 305 | READ | 305 lines, 3 describes. Covers the inventory's SQL against real Postgres — the lateral joins, the deterministic ordering, the empty corpus — none of which a fake DataSource could assert (:28-41). Rows are inserted directly rather than driven through the upload workflow, because the report reads the database and the test needs states the happy path cannot produce, such as an ACTIVE document whose indexing failed (:37-40). Six documents cover every state the report must describe: healthy, never-indexed, wrong-provider, expired, superseded (version 2 current with version 1 chunks left behind) and DRAFT. Totals are asserted to agree with the rows above them, down to `chunksByProvider`. Two cases pin the determinism claim directly: titles are asserted sorted and a second request is asserted byte-identical via `JSON.stringify` (:273-279), and the body is asserted to contain no generation timestamp — which is what makes it diffable (:281-283). The access describe pins that the literal `/documents/inventory` path is not swallowed by `@Get(':id')`. |
| 131 | `apps/api/test/notifications.e2e-spec.ts` | ts | 117 | READ | Four cases pinning who may read a notification, against real Postgres. Added by this audit (93d82b1) — three of the four failed before the IsNull fix. |
| 132 | `apps/api/test/pagination.e2e-spec.ts` | ts | 84 | READ | Sixteen cases pinning 400-not-500 on malformed limit/offset, absence, well-formed values, and the absence of an ERROR:UNHANDLED row. Added by this audit (68f9511). |
| 133 | `apps/api/test/phi-screening.e2e-spec.ts` | ts | 373 | READ | 373 lines, 6 describes — the WI-1 proof. The header states what the unit specs cannot show: that after a real rejection through the real HTTP stack the text is in **no table**, not the one it was headed for and not the audit trail (:27-42). Every assertion queries the database directly, because a 400 with a row quietly written behind it would satisfy a status-code test and fail the actual requirement. The searches use `::text LIKE` over whole rows and whole jsonb documents rather than naming columns, because the requirement is "nowhere", not "not in the column we thought of" (:38-41). `waitForAudit`/`settleAudit` exist because `AuditService.record` is fire-and-forget: presence is polled for, absence asserted only after the trail stops growing, so "no row" means "none was written" rather than "none has landed yet" (:78-105). One case pins the structural half — no `HTTP:POST:/chat/ask` row exists either, because a guard throws before the interceptor chain, so there is no second code path that could carry the body into the trail (:166-174). The negative corpus is the gold set **and** the field set, joined deliberately: the first identifying-phrase pattern matched `patient id` as a prefix of "patient identifiers" and rejected an ordinary medication-safety question; fourteen hand-picked negatives missed it and the gold set caught it on the first full run (:221-250), so every case a hospital adds to the independent set strengthens this control at no extra cost. |
| 134 | `apps/api/test/rag-integrity.e2e-spec.ts` | ts | 320 | READ | 320 lines, 4 describes. Every claim is one a mocked repository cannot make: transaction isolation, a UNIQUE constraint, an advisory lock, and a SQL predicate that must agree with retrieval's governance filters (:30-37). Three concurrent `indexDocument` calls are asserted to leave exactly one chunk set — the failure being that DELETE-then-INSERT under READ COMMITTED lets each call delete rows the other has not committed and both insert, leaving two full sets, an inflated count, the same passage cited twice, and no error (:89-111). The UNIQUE constraint is tested separately as the backstop that still holds for a second API instance on another connection (:113-132). The coverage describe pins the `staleRetrievable`/`staleOrphaned` split across four states — live, expired, superseded, deactivated — and records why "staleChunks = 0" was previously unreachable: retrieval already excludes an expired document for unrelated reasons and `reindexAll()` only visits ACTIVE ones, so the combined total stayed non-zero with a startup warning no action could clear (:160-164). `reindexStale()` is asserted to touch only the stale document and drive the criterion to zero; single-document reindex is asserted to add **no** approval-history row; and one case pins that `/rag/reindex/stale` is not shadowed by `/rag/reindex/:documentId`. |
| 135 | `apps/api/test/rbac-and-dose.e2e-spec.ts` | ts | 326 | READ | 326 lines, 2 describes. The unit suite proves the guard computes the right answer from a fabricated context; this proves it is actually mounted on the real routes with permissions from a real JWT (:26-31). A nurse is refused `/users`, `/audit-logs`, `/analytics/overview` and `/chat/answers`; an auditor reads audit and analytics but is refused `/chat/ask` and `/dose/calculate`; download-url is 403 — from the guard, not 404, because the permission check happens first (:97-102). `POST /roles` and `PATCH /roles/:id` are asserted to answer **404**, pinning their removal. One case pins a defect no API test could have found: `AllExceptionsFilter` emitted the reason under `error` only, while the web and mobile wrappers read `message`, so every rejection reached users as "Request failed (400)" — found by a browser test, and now asserted as `error === message` (:146-153, :186-214). The dose describe walks draft → refused calculation → only-the-pharmacist-approves → calculation with the verbatim Arabic safety warning asserted twice, the single-dose cap, and implausible weights. |
| 136 | `apps/api/test/support/demo-corpus.ts` | ts | 53 | READ | 53 lines. Stands the four seeded documents up through the *real* workflow — upload → submit-review → approve → index — because anything short of ACTIVE is not a source and a corpus inserted straight into the tables would not prove what these specs exist to prove (:6-10). Extracted so the answer-quality and field-set harnesses share one builder rather than drifting apart (:12-14). Throws by name if a document ends anywhere but ACTIVE (:47-51). The comment at :16-18 records why documents must be built one at a time: `ctx.pdf.pages` is a single mutable slot on the extraction stub. |
| 137 | `apps/api/test/support/e2e-app.ts` | ts | 279 | READ | 279 lines. The harness. Three fakes, each a genuinely external boundary: `InMemoryStorageService` (:29-52), `StubPdfExtractionService` (:74-85) and `CapturingMailService` (:88-106). The extraction stub's docblock corrects the old claim that extraction could not run under jest, and records that the belief concealed an intermittent production bug (:63-68). `createE2eApp` (:216-259) boots the real `AppModule` and installs the same global pipeline `main.ts` does — helmet, the JSON cap, `ValidationPipe({whitelist, transform})`, `AllExceptionsFilter` — so the suite meets the wiring a production request meets, not a reduced harness (:210-215). `close()` waits 150 ms before tearing down because `AuditService.record()` is fire-and-forget and a request can return before its row lands (:250-256). `seedRolesAndUsers` is deliberately not the demo seeder, which would couple these tests to S3 uploads (:163-166), and hashes at cost 4 because the work factor is not what is under test. `TABLES` (:119-137) is ordered so CASCADE has nothing left to chase. |
| 138 | `apps/api/test/support/env.ts` | ts | 42 | READ | 42 lines, loaded via jest `setupFiles` so it runs before any application module is imported — which matters because `config/env.ts` binds `isProduction` at load and `config/data-source.ts` reads process.env when it builds connection options (:1-9). Every Postgres value falls back through `E2E_POSTGRES_*` → `POSTGRES_*` → a local default, so one suite runs against the CI service container and a local cluster. Rate limits are raised to 10000 because throttling has its own dedicated spec and unrelated assertions should not trip it (:22-25); lockout is lowered to 3 to keep that spec short. Providers are pinned to `mock` because the point is the governance wiring, not model quality. The last line deletes `AUTH_DEV_RETURN_RESET_TOKEN` explicitly, so the suite asserts the shipped default rather than a test-only relaxation (:40-42). |
| 139 | `apps/api/test/support/gold-set.ts` | ts | 192 | READ | 192 lines, 15 cases: 10 answerable and 5 that must be refused. The header states three limits before any number: a green run is evidence the plumbing works and **not** clinical acceptability; under `EMBEDDING_PROVIDER=mock` this measures *lexical* retrieval, so reading it as "the assistant understands paraphrase" would be dishonest; and the corpus is four demo documents, not a hospital library (:6-25). Every case carries a `rationale` field explaining why it earns its place (:45-46). Several are chosen as topic-versus-content discriminators — "hand hygiene" appears in three of four documents but only one states a duration (:91-93). One case records a measurement error rather than a code bug: an earlier version asserted `15 mg/kg` and reported a miss the assistant had not made, because the source phrases it `15 mg per kg` (:57-60). The five refusal cases are the ones the header calls most important, and each names the lexical trap it sets — paediatric adrenaline because the corpus contains weight-based dosing for a different drug entirely (:179-184), insulin because the corpus names it only as a high-alert example with no regimen (:186-191). |
| 140 | `apps/api/tsconfig.build.json` | json | 4 | READ | 4 lines. Extends the base and excludes `node_modules`, `dist`, every `*.spec.ts` and `test/`, so the shipped `dist/` carries no test code. |
| 141 | `apps/api/tsconfig.e2e.json` | json | 4 | READ | 4 lines. Extends the base and widens `include` to `["src", "test"]`, which is what lets the e2e specs import application modules directly. |
| 142 | `apps/api/tsconfig.json` | json | 23 | READ | 23 lines. CommonJS/ES2021, `strict: true` with `strictPropertyInitialization: false` (TypeORM entities are assigned by the driver), `emitDecoratorMetadata` and `experimentalDecorators` for Nest and class-validator, `include: ["src"]` only. |
| 143 | `apps/mobile/.gitignore` | gitignore | 1 | READ | 1 line: `dist/`. |
| 144 | `apps/mobile/App.tsx` | tsx | 179 | READ | 179 lines. The whole navigation shell — there is no router; `tab` state plus a switch. Boot restores the API-origin override **before** any request is issued, then the persisted language and stored session together (:47-58). `setOnSessionExpired` is registered so an unrecoverable 401 anywhere drops straight back to login rather than stranding the nurse on error strings (:60-69), and is cleared on unmount. Tabs are filtered by permission, and `activeTab` falls back to home if the current role cannot see the stored tab (:94-98) — a stored tab from a previous session cannot strand a different role on a forbidden screen. Logout revokes server-side first, then clears locally. A category tile on Home opens the chat **with its scope preserved** through `CATEGORY_ASSISTANT` (:32-36, :100-107). The header row uses `row(lang)` so it mirrors under Arabic. |
| 145 | `apps/mobile/app.json` | json | 20 | READ | 20 lines of Expo config: name, slug `bnp-decision-guard`, scheme `bnpguard`, portrait-locked, splash in the same deep teal as the web primary token, and matching iOS/Android identifiers `health.bnp.decisionguard`. |
| 146 | `apps/mobile/babel.config.js` | js | 6 | READ | 6 lines. `babel-preset-expo` with `api.cache(true)` — the Expo default, nothing custom. |
| 147 | `apps/mobile/eas.json` | json | 37 | READ | 37 lines. Three build profiles, each baking a different `EXPO_PUBLIC_API_URL`: development at `10.0.2.2:4000` (the Android emulator's host alias), preview and production at placeholder hospital hostnames. Production auto-increments the version; submit targets the Play internal track. |
| 148 | `apps/mobile/jest.config.js` | js | 31 | READ | 31 lines. Uses `testEnvironment: 'node'` rather than the `jest-expo` preset, and the docblock states both the reason and the limit (:1-11): the modules under test import no React Native components, so they need no RN runtime — only stand-ins for the two native storage modules — which keeps the suite fast and free of the Metro/Babel transform chain; and testing the screens would need `jest-expo` plus `@testing-library/react-native`, which is "a separate, larger piece of work and is not what this suite claims to cover". `moduleNameMapper` points the two native modules at the fakes in `test/mocks/`, and the transform override explains why it needs `tsconfig.spec.json` (:25-26). |
| 149 | `apps/mobile/package-lock.json` | json | 8627 | GENERATED-SKIPPED | npm lockfile — machine-generated dependency graph; not read line by line. Resolved versions are queried with `npm ls` / `jq` instead (see 06-DEPENDENCIES.md). |
| 150 | `apps/mobile/package.json` | json | 33 | READ | 33 lines. Expo ~57.0.15, React Native 0.86.2, React 19.2.3. Deliberately **not** an npm workspace — it installs on its own, which is why CI gives it a separate `npm ci` and its own audit gate. Scripts are `start`, `android`, `ios`, `typecheck` and `test`. Carries its own `overrides` block pinning `uuid` to `^11.1.1`. |
| 151 | `apps/mobile/src/api.spec.ts` | ts | 348 | READ | 348 lines, 8 describes, 22 cases. Every test loads a **fresh** copy of the module through `jest.resetModules()`, because `api.ts` keeps module-level state — the cached session, the `loaded` flag, the current origin — and the header notes this is also the only honest way to exercise `getSession`'s one-shot read (:5-12). `loadWithStoredSession` writes tokens directly into the fakes without going through `setSession`, mirroring an app restart where a previous run wrote them and the in-memory cache starts empty (:30-34). The load-bearing case is "keeps tokens out of plaintext storage", which asserts against the two **separate** mock stores — that separation is the whole point of keeping the fakes apart. Also pins: a half-written session is refused rather than composed into a broken one; unreadable storage reads as signed out rather than throwing; the legacy plaintext session is purged on first read; refresh replays exactly once and does not loop when the replay is also rejected; a 401 on an *unauthenticated* request is left alone; and `logoutEverywhere` still clears locally when revocation fails. |
| 152 | `apps/mobile/src/api.ts` | ts | 181 | READ | 181 lines. The mobile fetch wrapper, and the security-relevant half of the app. **Tokens go to the OS keychain/keystore via `expo-secure-store`; only the non-secret profile goes to AsyncStorage** (:18-24), and the comment records why they are stored individually rather than as one blob — SecureStore values should stay small against ~2KB platform limits. `getSession` performs a one-time migration that **drops** any pre-SecureStore plaintext session rather than trusting it, on the stated reasoning that the user re-authenticates once (:71-73), and composes a session only when all three parts are present (:79-82). `DEFAULT_API_URL` is baked at build time, and the login screen can override it at runtime because `EXPO_PUBLIC_API_URL` is fixed inside a built binary — without the override a single build could not be pointed at another environment (:10-12). `api()` mirrors the web wrapper: bearer token, `Content-Type` only with a body, one refresh-and-replay on 401, then `setSession(null)` plus an `onSessionExpired` callback so the user lands on the login screen rather than a screenful of error strings (:112-116). `logoutEverywhere` revokes server-side best-effort and clears locally regardless (:140-148). |
| 153 | `apps/mobile/src/components/BottomNav.tsx` | tsx | 67 | READ | 67 lines. Five permission-gated tabs in a typed table (:20-26), filtered by the caller's permission list exactly as the web shell does. The docblock records a deliberate divergence from the design: Figma's "الحوكمة / Governance" slot is replaced by the real Dose Calculator, because the governance screen's metrics (trust index, risk map, protocol coverage) have no backing API while dose calculation is a real clinical feature that was otherwise unreachable from the nav (:14-19). Labels are dictionary keys, and `row(lang)` flips the bar under RTL. |
| 154 | `apps/mobile/src/i18n.spec.ts` | ts | 74 | READ | 74 lines, 4 describes, 9 cases. Asserts key parity across both languages, no empty strings, and that `t()` answers in the requested language. The case worth naming is "keeps the governed clinical strings out of the dictionary" (:25-33): mobile is not an npm workspace so it **cannot** import the constants from `@bnp/shared`, and rather than hardcode the full strings — which would make the test a second copy of the thing it is protecting — it asserts on the distinctive middle fragments of each. `alignFor` is covered from three directions: content language overriding interface language, leading digits and punctuation being ignored, and the fallback when there is no strong character at all. |
| 155 | `apps/mobile/src/i18n.ts` | ts | 175 | READ | 175 lines. **Arabic-first**, the mirror image of the web dictionary's English default, because the Figma design is Arabic/RTL (:1-4). **53 keys in each language, verified equal.** Direction is derived from the active language rather than React Native's global `I18nManager`, and the header gives the reason: flipping `I18nManager` requires an app restart on Android, so a toggle the user can see take effect immediately is worth the explicit `flexDirection` handling in every screen (:5-9). It states, like the web copy, that the two governed clinical strings are **never** translated here. `row()` and `align()` (:152-159) are the React Native substitutes for CSS logical properties. `alignFor()` (:161-175) is the substitute for `dir="auto"`, and the docblock states the clinical case for it: the model replies in the language of the question, so a nurse can browse an English UI and receive an Arabic answer, and aligning that by interface language would render clinical text against its own reading direction. It matches on the first *strong* directional character, mirroring the HTML rule so leading digits or punctuation do not decide. |
| 156 | `apps/mobile/src/screens/AuditScreen.tsx` | tsx | 163 | READ | 163 lines. Read-only audit list behind the `audit:read` tab, so it is invisible to a nurse and reachable by an auditor or admin. Rows render action, actor and timestamp — the same three columns the web audit table leads with. |
| 157 | `apps/mobile/src/screens/ChatScreen.tsx` | tsx | 393 | READ | 393 lines, the largest mobile screen. A governed refusal is rendered **verbatim** in its own warning-toned bubble, visually distinct from an answer, with a secondary caption naming it a contextual refusal (:200-217) — the mobile equivalent of the web's `RefusalAnswer`. Answer text, every step and every warning are aligned with `alignFor(text, lang)` rather than the interface language (:224, :235, :260), which is the whole reason that helper exists: the model replies in the language of the question. The PHI warning sits above the composer (:359), matching the web app — stated before the field rather than after a rejection. |
| 158 | `apps/mobile/src/screens/DoseCalculatorScreen.tsx` | tsx | 201 | READ | 201 lines. The docblock states the division of responsibility precisely: `GET /dose/formulas` already returns only pharmacist-approved formulas to non-privileged callers and the API re-checks approval on calculate, so **this screen never has to decide what is safe to offer** (:31-34). `safetyWarning` is rendered verbatim, RTL, on every result regardless of the selected UI language (:36-37, :193-194) — the contractual string from `@bnp/shared`, never assembled on the device. |
| 159 | `apps/mobile/src/screens/HomeScreen.tsx` | tsx | 177 | READ | 177 lines. Session summary plus category tiles that open a scoped chat. Analytics counters are fetched only when the caller holds `analytics:read` (:42), matching the web dashboard. Displays the caller's first six permissions as chips — a governance affordance rather than decoration, since it lets a nurse see what her role actually grants. |
| 160 | `apps/mobile/src/screens/LoginScreen.tsx` | tsx | 272 | READ | 272 lines. Email/password plus the MFA challenge, and the server-URL field. Two decisions are documented and both matter: the server-URL field is **genuinely load-bearing** because `EXPO_PUBLIC_API_URL` is frozen inside a built binary, so without it one build cannot be pointed at staging versus production (:26-28); and the design's offline "دخول تجريبي" demo login is **deliberately not implemented, because it would surface fabricated clinical content** (:30-31). That is the same refusal-first principle as the API, applied to a design brief. |
| 161 | `apps/mobile/src/screens/PoliciesScreen.tsx` | tsx | 105 | READ | 105 lines. Read-only list of approved documents with a search field, reachable only from the `documents:read` tab. No download affordance exists at all on mobile, which is consistent with `DOCUMENTS_DOWNLOAD` being withheld from `NURSE_USER` in the RBAC matrix. |
| 162 | `apps/mobile/src/theme.ts` | ts | 177 | READ | 177 lines: a colour token map, radius and spacing scales, and 101 `StyleSheet` entries. The docblock is candid about provenance (:3-10): structure, spacing and component anatomy follow the Figma file (390×844 Arabic/RTL), but **Figma's exact colour values were not retrievable, so rather than guess the tokens stay on the product's existing brand teal** — the same `#0f766e` the web app uses as `--primary`. Carries dedicated `warn`/`warnBg` tokens, which is what the refusal and dose-safety surfaces render in. |
| 163 | `apps/mobile/test/mocks/async-storage.ts` | ts | 45 | READ | 45 lines. In-memory stand-in for the native AsyncStorage module, **plaintext by design** — the docblock says so explicitly, because this is where the non-secret profile lives and the tests assert nothing sensitive does (:1-8). `__failWith` exists to drive the catch branches in `loadApiUrl` and `getSession` that a happy-path fake could never reach. |
| 164 | `apps/mobile/test/mocks/expo-secure-store.ts` | ts | 32 | READ | 32 lines. Stand-in for the OS keychain. The docblock states the point is not merely to stop the native import from throwing: **keeping this store separate from the AsyncStorage mock is what lets a test assert that tokens land in the secure store and never in plaintext** (:6-9). |
| 165 | `apps/mobile/tsconfig.json` | json | 14 | READ | 14 lines, extends `expo/tsconfig.base` with `strict: true` and `jsx: react-native`. The `module: esnext` / `moduleResolution: bundler` pair carries a comment explaining it is a constraint, not a preference: bundler resolution is only valid alongside an ES module target, and the base config's default (ES2015) rejects the dynamic imports `api.spec.ts` uses to reload the module under test (:6-8). |
| 166 | `apps/mobile/tsconfig.spec.json` | json | 9 | READ | 9 lines. Overrides to `commonjs`/`node` and adds jest types — the counterpart to the note above, since ts-jest emits CommonJS and cannot use the Metro-oriented bundler resolution. |
| 167 | `apps/web/e2e-smoke.mjs` | mjs | 401 | READ | 401 lines, 13 numbered steps, driven by Playwright against the full Docker stack. The `check()` helper exists because an earlier revision only `console.log`-ed the RBAC counts, so the two assertions that matter most — a nurse seeing no admin navigation and no download buttons — could never turn the job red (:13-18). The design principle running through it is **paired inverse assertions**, stated outright at :51-54: the same locator that must find nothing for a nurse is asserted to find something for a knowledge manager, because "without it, a locator that silently matched nothing would make the nurse's hidden-nav assertions pass vacuously". The same pairing is applied to download buttons (step 5 vs step 11). Step 12 pairs `isDisabled()` with `isEnabled()` for the same reason: the create button is also disabled while roles are loading, so "disabled" alone would pass for the wrong reason (:321-323). Bad credentials use a deliberately non-existent account, because per-account lockout is real and a smoke run must not lock a demo login out of the next run (:226-228). Form fields are addressed by visible label rather than input index, so reordering cannot silently repoint "weight" at "concentration" (:59-61). Step 13 doubles as the only end-to-end proof that `AuditInterceptor` recorded step 12's mutations, and asserts the action filter is exact rather than a substring by requiring `AUTH:LOGIN` to be absent. |
| 168 | `apps/web/next-env.d.ts` | ts | 7 | READ | 7 lines, generated by Next and marked not-to-be-edited. References `next`, `next/image-types/global`, and the generated route and root-param type files under `.next/types`. |
| 169 | `apps/web/next.config.mjs` | mjs | 7 | READ | 7 lines. `output: 'standalone'` (what `Dockerfile.web` copies) and `transpilePackages: ['@bnp/shared']`, which is what lets the web app import the workspace package's TypeScript rather than requiring a separate build step for it. |
| 170 | `apps/web/package.json` | json | 28 | READ | 28 lines. Three scripts (`dev`, `build`, `start`). Runtime dependencies are `next@^16.3.3` — the floor raised during the security work, and the package whose nested lockfile entry defeated an earlier bump — plus React 18, `qrcode` (the MFA enrolment QR). `playwright` is a devDependency here because the browser smoke test lives in this workspace. |
| 171 | `apps/web/postcss.config.mjs` | mjs | 6 | READ | 6 lines. Tailwind plus autoprefixer, nothing else. |
| 172 | `apps/web/public/.gitkeep` | gitkeep | 0 | READ | Empty (0 bytes). Keeps `public/` in git so the Next build has the directory. |
| 173 | `apps/web/src/app/(app)/analytics/page.tsx` | tsx | 212 | READ | 216 lines. Counters grouped by the question they answer rather than thirteen identical cards, with the reasoning stated: a wall of equal-weight KPI tiles is decoration, because it tells you nothing about which number matters (:25-29). The refusal rate gets its own panel rather than a tile among twelve, because it is the one number describing whether governance is working (:146-147), and the caption beneath it says a refusal is a correct outcome and that a rising rate points at gaps in the library rather than a broken assistant. `Bars` marks the bar itself `aria-hidden` and puts the figure in text beside it, so a screen reader gets the number without a chart to interpret (:84-85). Group and metric labels are dictionary keys in a typed table (:30-65). |
| 174 | `apps/web/src/app/(app)/answer-review/page.tsx` | tsx | 236 | READ | 235 lines. The committee surface behind `ai:review-answers`. Three tabs via `SegmentedControl`, each with its own empty copy in `EMPTY_COPY` (:50-65) — and the UNREVIEWED description states a governance fact worth keeping: refusals need no review, since no clinical claim was made (:54). The question leads the card because it is what the reviewer judges the answer against (:135-136 comment). Answer, steps, warnings and question all carry `dir="auto"`. Approve and flag are the only actions, shown only on the UNREVIEWED tab. This file holds the largest single cluster of untranslated English strings in the web app — the tab labels, all six empty-state strings and several inline fragments — recorded in 09-GAPS.md. |
| 175 | `apps/web/src/app/(app)/approvals/page.tsx` | tsx | 392 | READ | 391 lines. The governance workflow screen. `needsAction` (:60-67) is the whole design: a document needs a human when it is mid-workflow, **and** the viewer holds the permission for that particular transition — so the "Needs your action" count is per-role rather than a global backlog. `LifecycleTrack` (:54-72) renders DRAFT→IN_REVIEW→APPROVED→INDEXED→ACTIVE as progress pips and returns null for the derailed states (REJECTED, EXPIRED, INACTIVE) rather than drawing a track that does not apply. Every action button is gated on the matching permission, matching the server. Expanding a row discloses two records at once — approval events and physical uploads — because "what happened to the document" and "what the document was" are different questions (:96-97). The rejection reason is captured inline, with the comment recording why `window.prompt` was removed: it blocked the page, could not be styled, and is suppressed outright in some embedded browsers, losing the reason silently. |
| 176 | `apps/web/src/app/(app)/assistant/page.tsx` | tsx | 23 | READ | 23 lines. `PageHeader` + `AssistantChat assistantType="NURSING"` + `ChatHistory`. All strings come through `useT()`; the page holds no logic of its own. |
| 177 | `apps/web/src/app/(app)/audit/page.tsx` | tsx | 172 | READ | 172 lines. Filterable compliance surface. Both filters run through `useDebounced` and reset `offset` to 0 on change (:68-71), so paging cannot strand the reader on page 3 of a new filter. The error branch carries the reasoning that matters for this screen: a compliance surface rendering blank on failure is worse than useless, because an empty table reads as "no events occurred" (:101-102). The empty state distinguishes "no events match this filter" from "no events at all". `refreshing` dims the panel rather than replacing it with a skeleton. Resource moves inline on narrow viewports so nothing is silently lost on a phone (:133-139); metadata is truncated with the full JSON in a `title`. |
| 178 | `apps/web/src/app/(app)/cbahi/page.tsx` | tsx | 120 | READ | 120 lines. Semantic search over the CBAHI category via `GET /rag/search`, rendering chunks rather than answers. Four distinct states are distinguished: not-yet-searched, no match for a named term, results, and error. The error branch carries a comment recording what it replaced — a `finally` that swallowed the failure entirely, leaving the page looking like the search had returned nothing (:46-47). Snippets and titles use `dir="auto"`. |
| 179 | `apps/web/src/app/(app)/dashboard/page.tsx` | tsx | 254 | READ | 254 lines. Governance state framed as work rather than metrics: the comment at :88-90 says it is deliberately not a wall of KPI cards, and each `AttentionRow` resolves to either a success badge reading "clear" or a link to the thing that needs attention (:196, :224-233). The analytics block is fetched only when the caller holds `analytics:read`, with the fetcher resolving to null otherwise (:64-70), so a nurse's dashboard makes one request fewer rather than a forbidden one. Quick actions are permission-filtered from a keyed table (:27-57, :153). The arrow glyph carries `rtl:-scale-x-100` so it points the right way in Arabic (:163). The page closes on the governance guarantee card, which prints `REFUSAL_MESSAGE_AR` from `@bnp/shared` — force-RTL with a comment explaining why `text-right` rather than `text-end` (:179-186). |
| 180 | `apps/web/src/app/(app)/dose-calculator/page.tsx` | tsx | 333 | READ | 333 lines. `GET /dose/formulas` returns only approved formulas for a plain `dose:calculate` holder, so the picker cannot offer an unapproved one — the server-side gate is what enforces it, and the screen simply has nothing else to show. Weight is the only always-required input and the submit button is disabled on that alone, because everything else has a server-side default and the button should stay honest about what is actually missing (:75-78). The result renders the server's `steps` array verbatim rather than recomputing anything client-side, so the arithmetic a nurse reads is the arithmetic the API performed and audited. `safetyWarning` is printed last, force-RTL, straight from the response — it is `DOSE_SAFETY_WARNING_AR` from `@bnp/shared`, and the comment explains `text-right` over `text-end` (:308-310). Warnings from the cap logic get their own danger block above it. Embeds `FormulaManager`, which renders nothing for unprivileged callers. |
| 181 | `apps/web/src/app/(app)/drug-prep/page.tsx` | tsx | 21 | READ | 21 lines. The same composition as the assistant page with `assistantType="DRUG_PREPARATION"` and no history panel — the two assistant surfaces differ only by that prop and their copy. |
| 182 | `apps/web/src/app/(app)/layout.tsx` | tsx | 5 | READ | 5 lines. Wraps every route in the `(app)` group in `AppShell`, which is where the permission-filtered navigation lives. The route group is what keeps `/login` outside the shell. |
| 183 | `apps/web/src/app/(app)/notifications/page.tsx` | tsx | 101 | READ | 101 lines. First UI for rows the daily governance cron has been writing since day one (:28-33). Title and message carry `dir="auto"` because although the cron writes English, the text embeds document titles which can be Arabic. Full `useAsyncData` lifecycle plus a separate `actionError` for the mark-read POST, so a failed action does not blank the list. |
| 184 | `apps/web/src/app/(app)/policies/page.tsx` | tsx | 208 | READ | 211 lines. The nurse-facing library view, fixed to `status: ACTIVE` in the query (:67) — this screen shows what the assistant can actually cite, not the whole corpus. `expiryTone` (:40-47) colours a row amber within 30 days of expiry and red past it, on the stated grounds that those documents stop being answerable soon. Changing a filter resets `offset` in an effect, because a stale offset past the filtered total would render an empty page with no way back (:61-63). The download column and its button render only for `documents:download` holders, and the note explaining the absence is shown to everyone else — the copy-protection rule made visible rather than silent. Narrow viewports fold category and version under the title so nothing is lost. |
| 185 | `apps/web/src/app/(app)/security/page.tsx` | tsx | 251 | READ | 251 lines. Self-service MFA — the API has had enroll/enable/disable since Phase 9a, and this page is what finally lets a user reach it (:32-38). The enrolment secret lives in component state only: it is never written to storage, and abandoning the page simply leaves the account un-enrolled, because `enableMfa` is what flips the flag. The QR is rendered from the `otpauth://` URI entirely client-side, so the secret never leaves the browser again after the enroll response (:52-62); a plain `<img>` is used with a comment noting `next/image`'s remote-loader machinery has nothing to add to a local data URL (:180-181). The secret is also shown as selectable text for authenticators that cannot scan. Disabling requires the account password, matching the server-side rule. Every string on this screen comes from the dictionary. |
| 186 | `apps/web/src/app/(app)/settings/page.tsx` | tsx | 373 | READ | 372 lines. Operator surface. `canReindex` is derived from `documents:index` and is deliberately independent of `canManage`/`settings:manage`, with the comment noting the two capabilities do not imply each other in the role matrix (:30-33) — so the reindex controls appear for the role that can actually run them. Three operator actions are exposed: full reindex, `reindexStale`, and the provider check, each rendering its own outcome including the failed-document list with per-document errors. Settings values are edited as raw JSON with a validity check that blocks saving and explains the rule. A read-only holder sees the values and a line saying the role cannot change them. Two strings here stay English on purpose because they name `EMBEDDING_PROVIDER` and the `staleRetrievable` field. |
| 187 | `apps/web/src/app/(app)/upload/page.tsx` | tsx | 159 | READ | 159 lines. Client-side validation rejects a non-PDF and anything over 25 MB before the upload starts, so the nurse learns the file is wrong before sending it (:34-48); the 25 MB cap matches the server's `FileInterceptor` limit. `FormData` is passed to `api()` directly, with a comment noting that `api()` forces `Content-Type` only for string bodies, so the browser-generated multipart boundary survives and the upload still gets refresh-on-401 (:62-64). Expiry is validated as strictly future both by `min={today}` and by an explicit check. Several strings on this screen were English literals — see the i18n finding in 09-GAPS.md. |
| 188 | `apps/web/src/app/(app)/users/page.tsx` | tsx | 291 | READ | 291 lines. Provisioning and role assignment. `/roles` is fetched only when the caller holds `roles:read`, resolving to an empty array otherwise (:52-58), so the two requests are issued together but neither is made to be refused. The roles dropdown is populated from the API rather than a local list, so it cannot drift from the seeded rows. Deactivation is a soft toggle matching the API's soft delete. `lastLoginAt` is formatted with `localeTag(lang)`, which pins `latn` digits under Arabic. Email folds under the name on narrow viewports. Three of its strings were English until this branch — the create and update failures and the short-password hint. |
| 189 | `apps/web/src/app/globals.css` | css | 181 | READ | 164+ lines. Declares colours as **roles** (surface, elevated, sunken, border, muted, primary) rather than palette steps, so dark mode re-maps meaning instead of inverting greys — stated as the tell of a bolted-on dark mode (:5-12). The dark block says outright it is not an inversion: surfaces get *lighter* with elevation and accents are lifted (:56-57). Two contrast figures are recorded as measured rather than assumed — the light text ramp at 17.5:1/7.6:1/4.8:1 (:25-26), and `--text-subtle` in dark pinned at 142/137/132 because the more obvious 130/125/120 measured 4.35 and failed AA at these sizes (:68-69). Tabular numerals are applied to `.tnum` and every table cell, so figures line up for scanning (:102-107). `:focus-visible` gives one always-visible ring to keyboard users only (:109-115), and `prefers-reduced-motion` is honoured globally so no component has to remember (:117-127). The skip link is the first tab stop, visible only on focus. |
| 190 | `apps/web/src/app/layout.tsx` | tsx | 65 | READ | 65 lines. Carries the two before-paint init scripts. `THEME_INIT` (:17-23) reads `bnp.theme` and falls back to `prefers-color-scheme`, because without it the page renders light and snaps dark on hydration — and routes outside the app shell, `/login` above all, would ignore the preference entirely (:12-15). `LANG_INIT` (:34-43) is the same trick for a stronger reason, stated at :25-31: direction has to be right in the very first paint, since setting it from a React effect would render the whole shell LTR and flip it on hydration, moving every element. `<html>` deliberately carries **no** static `lang`/`dir` (:51-53) so a stored Arabic preference is honoured on the first render, with `suppressHydrationWarning` as the consequence. Providers nest `LanguageProvider` outside `AuthProvider`. |
| 191 | `apps/web/src/app/login/forgot/page.tsx` | tsx | 215 | READ | 214 lines. Two forms in one route: request a link, and set a new password from a token. `linkToken` from the query string collapses it to the second form, so a user arriving from the email does not see a request box they have already used. The module-level `post()` helper is outside any component, which is why its `Request failed` fallback is one of the justified English literals in 09-GAPS.md. The `Suspense` boundary is required rather than decorative — the comment records that `useSearchParams` opts the tree into client rendering, and without the boundary the whole route fails to prerender at build time (:193-197). Its fallback string was the last untranslated one and is now keyed. |
| 192 | `apps/web/src/app/login/page.tsx` | tsx | 173 | READ | 173 lines. Login and the MFA challenge in one form that swaps its fields. The docblock records a real credential exposure it closed: the demo email used to be prefilled and its password rendered in plain text on the login page, shipped in every build and visible to any unauthenticated visitor of a live system — "you did not even need to find the repository to sign in" (:15-18). The replacement takes the address from `NEXT_PUBLIC_DEMO_EMAIL`, and the comment explains why a literal behind an `if` would not have been enough: `NEXT_PUBLIC_*` is inlined into the client bundle, so a gated literal is still a credential shipped to every browser (:20-23). No password appears in any form. Focus moves to the MFA input when the form swaps, because a keyboard user would otherwise be left on a button that no longer exists (:37-41). Login uses a full `window.location.href` navigation so `AuthProvider` mounts fresh with the new session (:62-63). The one remaining English literal in the web app is here at :166 — the demo hint, which renders only when the env var is set and so never in a deployment. |
| 193 | `apps/web/src/app/page.tsx` | tsx | 13 | READ | 13 lines, client component. The root route is a redirect only: `/dashboard` when a session exists in localStorage, `/login` otherwise. Renders `null` — there is no landing page. |
| 194 | `apps/web/src/components/assistant-chat.tsx` | tsx | 387 | READ | 388 lines. The clinical answer surface. A refusal gets its own component and its own treatment — warning tone, an explicit badge, and **no** confidence meter or citation list — so it can never be mistaken for guidance, and the governed Arabic string is rendered verbatim with `dir="rtl" lang="ar"` (:62-68, :143-153). `RefusalReason` (:76-132) is shown only when the API supplied `diagnostics`, which it omits entirely for roles without `analytics:read`; it explains which of the four gates fired so a knowledge manager can tell a corpus gap from an over-tight threshold, since those need opposite fixes (:70-75). Those four explanations stay in English on purpose, with the reason written down: they are governance prose naming English configuration keys a reader has to match against env vars and logs, so translating the chrome around them while leaving these greppable is the deliberate choice (:78-83). `consideredSources` renders the raw extracted text in monospace, so garbled extraction or a contents-page heading is visible rather than inferred (:119-124). Answers use `dir="auto"` on every free-text node because the model replies in the question's language (:170-174), a steps-only answer renders without an empty summary paragraph, and the PHI warning sits **above** the input — stated before the field rather than after a rejection, because a blocked request has already been composed and the habit is what carries to the next question (:357-364). |
| 195 | `apps/web/src/components/chat-history.tsx` | tsx | 116 | READ | 116 lines. The caller's own past questions from `GET /chat/history`, which the server scopes to the JWT's userId. Collapsed by default and fetched only on first expand, so the assistant page stays a single-request screen for the common ask-and-go visit (:30-36) — implemented by a fetcher that resolves to an empty list while closed (:41-47). Question and answer text carry `dir="auto"` because both come back in whatever language they were typed or answered in, independent of UI language. Uses the full `useAsyncData` lifecycle: skeleton, `ErrorState` with retry, empty state, then the list. |
| 196 | `apps/web/src/components/formula-manager.tsx` | tsx | 335 | READ | 335 lines. Pharmacist-facing formula governance. The docblock records that the API — create as DRAFT, pharmacist approval before the calculator will touch it — existed with no UI, and that this component is what makes the workflow reachable; it also notes the server re-checks both permissions regardless (:58-65). The fetch is the interesting part: hooks must run unconditionally, so an unprivileged caller resolves to an empty list **without touching the network** and the render bails out below, meaning a nurse opening the calculator does not pay for a second formulas request (:73-77). Approve is offered only on DRAFT rows and only to `dose:formulas-approve` holders. Formula and drug names carry `dir="auto"`, since a pharmacist may enter either language. |
| 197 | `apps/web/src/components/language-toggle.tsx` | tsx | 34 | READ | 34 lines. EN / عربي switch, deliberately the same shape as `ThemeToggle` including the fixed-size pre-mount placeholder so the toolbar does not shift (:6-9). Shows the language you would switch **to**, labelled in that language — the convention a reader of either language can act on without translating the control first (:11-13). |
| 198 | `apps/web/src/components/shell.tsx` | tsx | 307 | READ | 307 lines. `NAV_GROUPS` (:23-65) is four groups of twelve destinations, grouped by intent because a single flat column forces users to read every label to find anything (:14-16). Labels are dictionary **keys**, not literals, while hrefs are deliberately not localised so bookmarks, the smoke test and the Railway healthcheck survive a language change (:17-21). Each item carries an optional permission and the list is filtered by `hasPermission` before render (:147-150); `/security` deliberately has none, because every authenticated user manages their own MFA (:58-60). The unread-notification count is one fetch per mount with no polling, and a failure degrades to no badge (:77-91). The auth redirect checks `localStorage` directly, because provider state can lag a client-side navigation straight after login (:93-97). The mobile drawer is a real modal surface: `role="dialog"`, `aria-modal`, Esc closes, Tab is trapped between first and last focusable, and focus returns to the opener on close (:102-133). Layout uses logical properties throughout (`inset-y-0 start-0`, `border-e`, `ps-`/`pe-`) so RTL mirrors. Exports `STATUS_TONES` as the single lifecycle→tone mapping (:289-301). |
| 199 | `apps/web/src/components/theme-toggle.tsx` | tsx | 58 | READ | 55 lines. Resolves the stored `bnp.theme` preference, falling back to `prefers-color-scheme` (:8-14), toggles the `dark` class on `<html>` and persists on change. Renders a same-size placeholder until mounted, for the same anti-shift reason as the language toggle (:32-33). Two inline SVGs, both `aria-hidden` with the accessible name on the button. |
| 200 | `apps/web/src/components/ui/index.tsx` | tsx | 592 | READ | 571+ lines, read in four consecutive chunks — the design system every screen composes from. `cx` (:16-18) is the class joiner. `Button` keeps a `loading` button mounted and sized to avoid layout shift and marks it `aria-busy`, so assistive tech reports progress instead of the label vanishing (:45-49). `Field` (:107-153) generates one id and wires `aria-describedby`/`aria-invalid` through context to whichever control it wraps, with error taking precedence over hint so a screen reader hears the blocker first (:125-126). Three surfaces are distinguished on purpose so the UI is not "card everything": Section, Card and Panel (:278-283). `SkeletonRows` pairs visual shimmer with `role="status"` and an `sr-only` label. `EmptyState` is documented as explaining what to do next, never a bare "No data" (:369-372). `SegmentedControl` renders buttons with `aria-pressed` rather than styled links, because the previous colour-only treatment conveyed nothing to keyboard or screen-reader users (:455-459). `Pagination` announces its range via `aria-live`, since the only previous feedback that Next/Previous did anything was rows silently changing (:506-509). Two components in this file hardcoded English until this audit — see the note in 07-QUALITY-AND-RISKS and the fix in this commit. |
| 201 | `apps/web/src/lib/api.ts` | ts | 92 | READ | 92 lines. The fetch wrapper. `API_URL` reads `NEXT_PUBLIC_API_URL`, which is baked in at Docker build time rather than read at runtime. Session lives in `localStorage` under `bnp.session`, and `getSession` returns null rather than throwing on unparseable JSON or a server render (:20-28). `api()` attaches the bearer token, sets `Content-Type` only for string bodies, and on a 401 refreshes once and retries with `retryOn401: false` — then, if the refresh failed, clears the session and hard-navigates to `/login` (:72-77). The error path reads `body.message`, joining an array (class-validator returns one), which is the client half of the `message`/`error` fix pinned in `rbac-and-dose.e2e-spec.ts`; `Request failed (N)` remains only as the fallback when no JSON body can be read (:79-90). |
| 202 | `apps/web/src/lib/async.ts` | ts | 91 | READ | 89 lines. `useAsyncData` is the load/render lifecycle every data screen uses. Its docblock records what it replaced: `api(...).then(setState)` with no error and no loading branch, so a failed request rendered an empty table with no explanation (:18-21). Two correctness details are called out and implemented — a monotonic `seq` guard drops stale responses, because filters change per keystroke and a slow early response can otherwise land after a fast later one and show results for a filter the user has left (:23-26); and `loading` is distinguished from `refreshing` by whether data has ever arrived, so re-filtering does not flash a skeleton over a populated list (:27-28, :72-73). `useDebounced` (:82-89) exists because every keystroke in the audit and policy filters previously fired its own request. |
| 203 | `apps/web/src/lib/auth.tsx` | tsx | 62 | READ | 62 lines. `AuthProvider` re-reads the session on every navigation, because login and logout mutate `localStorage` directly and provider state would otherwise go stale (:33-38). `logout()` fires `POST /auth/logout` first — bumping `token_version` invalidates every outstanding refresh token, not just this tab's copy — but deliberately does not await it, so local cleanup cannot hang on or fail with the network call (:40-50). `hasPermission` reads the permission list the JWT carried. |
| 204 | `apps/web/src/lib/i18n.ts` | ts | 1044 | READ | 1044 lines, read in five consecutive chunks — the bilingual dictionary plus five helpers. **382 keys in each language, verified equal** by extracting both key sets and differencing them: neither block has a key the other lacks. That parity is enforced by the compiler, not by convention — deleting one Arabic key and running `tsc` fails with `TS7053` at the `t()` indexer (:1010-1017), confirmed by doing it. The header explains two deliberate divergences from `apps/mobile/src/i18n.ts`, which it otherwise mirrors (:1-25): English is the default here while mobile is Arabic-first, because the web app is already live and its governance screens skew English-speaking, so English-default changes nothing for a current user until they opt in; and there are no `row()`/`align()` helpers because the browser does natively what React Native cannot — `dir="rtl"` flips layout and `dir="auto"` picks direction from content. It states outright that the two governed clinical strings are **never** in this dictionary: they come from `@bnp/shared` verbatim, and translating them here would fork the safety contract the API tests assert on (:22-24). `t()` interpolates `{name}` placeholders rather than concatenating, because word order differs between the languages — "Welcome, {name}" and "{name} أهلاً بك يا" put the name in different places — and leaves an unknown placeholder visible so a typo shows up rather than producing a sentence with a hole in it (:1001-1009). `localeTag()` pins `ar-u-nu-latn` with the clinical reason stated: Arabic locales default to Eastern Arabic-Indic digits, which are correct for prose but wrong for dose figures, version numbers, page citations and timestamps that clinicians cross-check against English source PDFs (:1028-1036). |
| 205 | `apps/web/src/lib/language.tsx` | tsx | 106 | READ | 106 lines. The language provider. Its docblock is the argument for not using locale routing: every route is statically prerendered and sits behind auth, so locale-in-URL would buy no SEO while restructuring all 13 routes and breaking both the browser smoke test's paths and the Railway `/login` healthcheck (:51-62). `readStoredLang` catches blocked storage and falls back to English rather than taking the app down over a preference (:45-48), and the persist effect swallows the same failure (:78-82). `mounted` is false until the stored preference has been read, which is what lets the toggles render a fixed-size placeholder instead of shifting the toolbar. |
| 206 | `apps/web/tailwind.config.ts` | ts | 66 | READ | 66 lines. `darkMode: 'class'` — paired with the `THEME_INIT` script in `app/layout.tsx`. Every colour is a CSS-variable RGB triplet wrapped by `token()` so it still accepts Tailwind's `/opacity` syntax (:3-4), which is what lets one class set serve both themes. The radius scale is documented as encoding hierarchy rather than a single rounded look — controls tighter than the containers holding them (:32-40) — and the type scale bakes line-height and tracking into each step. |
| 207 | `apps/web/tsconfig.json` | json | 30 | READ | 30 lines, `strict: true`, `noEmit`, bundler resolution, `@/*` path alias. Two options carry comments explaining they are constraints rather than preferences: `jsx: react-jsx` because Next 16 rejects `preserve` outright (:13), and `target: ES2017` because Next only polyfills top-level await to the esmodules target, making this the floor (:15-17). |
| 208 | `docker-compose.yml` | yml | 137 | READ | Local full-stack definition, five services: `postgres` (`pgvector/pgvector:pg16`, initdb mount, pg_isready healthcheck, :4-20), `minio` (:22-38), `minio-init` (`minio/mc`, runs `mc mb`, :40-53), `api` (built from Dockerfile.api, 30 env vars, node-based /health/ready healthcheck with 60s start_period, :55-116) and `web` (build ARG `NEXT_PUBLIC_API_URL`, :118-129). `NODE_ENV` defaults to `development` here deliberately (:66-71). |
| 209 | `docs/api.md` | md | 202 | PENDING | — |
| 210 | `docs/architecture.md` | md | 105 | PENDING | — |
| 211 | `docs/clinical-validation.md` | md | 241 | PENDING | — |
| 212 | `docs/database-schema.md` | md | 63 | PENDING | — |
| 213 | `docs/production-readiness.md` | md | 494 | PENDING | — |
| 214 | `eslint.config.js` | js | 94 | READ | ESLint 9 flat config for the whole monorepo. Ignores `apps/mobile/**` and build output (:21-28); enables `js.configs.recommended` + `tseslint.configs.recommended` (:31-32); sets `no-unused-vars` to error with `^_` exemptions and `no-explicit-any` to warn (:39-46); adds react-hooks rules for `apps/web/**` (:52-58); relaxes three rules for spec files (:66-73); and grants Node + `document`/`window` globals to `**/*.mjs` because `page.evaluate` bodies run in the browser (:82-93). |
| 215 | `infra/docker/Dockerfile.api` | api | 64 | READ | Two-stage API image. Build on `node:22-alpine` installs only the shared+api workspaces (:2-10); runtime copies `dist` and `node_modules`, sets `NODE_ENV=production`, exposes 4000 (:13-23). The CMD (:64) chains migrate → optional create-admin (fatal on failure) → optional seed (gated on NODE_ENV, non-fatal) → `node dist/main.js`; the 40-line comment above it records the 2026-08-22 incident where a 9-character ADMIN_PASSWORD left zero active users (:24-63). |
| 216 | `infra/docker/Dockerfile.web` | web | 24 | READ | Two-stage web image. Build stage bakes `NEXT_PUBLIC_API_URL` as an ARG into the bundle (:4-5) — the reason changing it needs a rebuild; runtime copies the Next standalone output and serves `apps/web/server.js` on 3000 (:15-24). |
| 217 | `infra/docker/initdb/01-pgvector.sql` | sql | 2 | READ | Two statements run by the Postgres container on first init: `CREATE EXTENSION IF NOT EXISTS vector` and `\"uuid-ossp\"` (:1-2). |
| 218 | `infra/k8s/README.md` | md | 78 | READ | Operator guide for the k8s manifests: a file table (:6-11), the three-origins failure mode (:13-25), five pre-apply steps (:27-42), a nine-row table of what the manifests deliberately do not do — images, secret management, Postgres, object storage, TLS, the in-process expiry cron under replicas:2, backups, observability, NetworkPolicy/HPA/PDB (:44-60) — and the apply order (:62-71). |
| 219 | `infra/k8s/api-deployment.yaml` | yaml | 79 | READ | Reference API Deployment (2 replicas) + Service. `envFrom` the `bnp-secrets` Secret (:18-19); sets NODE_ENV=production, CORS_ORIGINS, MAIL_PROVIDER=smtp, mock LLM/embedding providers and SEED_ON_BOOT=false (:20-39); readinessProbe on `/health/ready` and livenessProbe on `/health`, deliberately different endpoints (:40-54); Service maps port 80 to 4000 (:59-67). |
| 220 | `infra/k8s/ingress.yaml` | yaml | 66 | READ | Two Ingress objects — web on `app.your-hospital.example` (:17-39) and API on `api.your-hospital.example` (:41-66) — with cert-manager annotations, a 1m body cap for web and 32m plus a 120s read timeout for the API because ingestion runs inside the request (:47-51). The header states the three origins that must agree (:7-13). |
| 221 | `infra/k8s/secrets.example.yaml` | yaml | 27 | READ | Opaque Secret template with 16 `stringData` keys — Postgres, both JWT secrets, S3, OPENAI_API_KEY and the four MAIL_* values — every sensitive one set to the literal `REPLACE_ME` (:6-24). No real credential is present. |
| 222 | `infra/k8s/web-deployment.yaml` | yaml | 41 | READ | Reference web Deployment (2 replicas) + Service, both probes on `/login` because it is statically prerendered and does not call the API (:18-28); Service maps 80 to 3000 (:33-41). |
| 223 | `infra/railway/README.md` | md | 59 | READ | Documents the actual live deployment: project `bnp-decisionguard`, four services with build sources, domains and healthcheck paths (:26-33); why the config is documented rather than committed as `railway.json` (:11-24); the env-var names per service (:35-46); and two known gaps — `openai` providers rather than `mock`, and single replica/region (:53-59). |
| 224 | `package-lock.json` | json | 11272 | GENERATED-SKIPPED | npm lockfile — machine-generated dependency graph; not read line by line. Resolved versions are queried with `npm ls` / `jq` instead (see 06-DEPENDENCIES.md). |
| 225 | `package.json` | json | 42 | READ | Monorepo root manifest. Declares npm workspaces `packages/shared`, `apps/api`, `apps/web` (:6-10) — `apps/mobile` is absent, so it is not a workspace. 14 scripts (:11-25), `engines.node >=20` (:26-28), five `overrides` pinning lodash/multer/file-type/@nestjs (:29-35), and four ESLint devDependencies (:36-41). |
| 226 | `packages/shared/package.json` | json | 13 | READ | Manifest for `@bnp/shared`: private, `main`/`types` point at `dist/` (:5-6) — the reason api and web fail until `build:shared` runs. One script (`tsc -p tsconfig.json`, :8) and one devDependency, typescript ^5.5.4 (:10-12). No runtime dependencies. |
| 227 | `packages/shared/src/constants.ts` | ts | 83 | READ | The clinical contract plus seven enums. Three verbatim Arabic strings — `REFUSAL_MESSAGE_AR` (:5-6), `DOSE_SAFETY_WARNING_AR` (:8-9) and `PHI_REJECTION_MESSAGE_AR` (:18-19) — then `PLATFORM_NAME` (:21) and the enums `DocumentCategory` 5 values (:22-28), `DocumentStatus` 8 (:30-39), `ApprovalAction` 7 (:41-49), `ConfidenceLevel` 4 (:51-56), `AssistantType` 3 (:58-62), `DoseFormulaStatus` 3 (:64-68), `DoseFormulaType` 3 (:70-74) and `DoseRoute` 6 (:76-83). |
| 228 | `packages/shared/src/index.ts` | ts | 11 | READ | Barrel re-exporting `./constants`, `./phi` and `./rbac` (:1-3). The comment (:5-11) records that a former `types.ts` of eight DTO interfaces was deleted after a sweep found zero consumers. |
| 229 | `packages/shared/src/phi.ts` | ts | 159 | READ | Pure PHI scanner with no DB, request or logger. Exports `PhiCategory` 5 values (:16-27) and `PhiProfile` 2 (:44-47); `METADATA_CATEGORIES` limits the metadata profile to NATIONAL_ID/PHONE/MRN (:49-53). Four built-in patterns: NATIONAL_ID ten digits starting 1 or 2 (:68), DATE_OF_BIRTH full numeric date either order (:75-76), PHONE Saudi mobile in three forms (:79), IDENTIFYING_CONTEXT Arabic and English phrases each needing a trailing value (:98-99). `foldDigits` maps Arabic-Indic and Extended Arabic-Indic digits to ASCII before matching (:114-120). `scanForPhi(text, options)` returns every matching category and never any part of the text (:139-159); the MRN pattern runs only when supplied (:153-156). |
| 230 | `packages/shared/src/rbac.ts` | ts | 141 | READ | The RBAC matrix, single source of truth. `RoleName` 7 roles (:1-9); `Permission` 22 values across users/roles, documents, AI, dose, governance (:11-42) with a comment explaining why no ROLES_MANAGE exists (:16-18); `CLINICAL_READ` bundle of 5 (:46-52); `ROLE_PERMISSIONS` mapping every role (:69-122) — SUPER_ADMIN gets all, NURSE_USER exactly CLINICAL_READ (:115), AUDITOR 4 read permissions (:116-121); `ROLE_DESCRIPTIONS` (:124-132); and `permissionsForRoles(roles)` which unions via a Set and silently ignores unknown roles (:134-141). |
| 231 | `packages/shared/tsconfig.json` | json | 13 | READ | TypeScript config for the shared package: target ES2021, commonjs, `declaration: true`, `outDir: dist`, `rootDir: src`, `strict: true` (:2-11), including only `src` (:12). |


**TOTAL FILES ON DISK: 230 | DOCUMENTED IN INDEX: 230 | MATCH: ✅**

> The denominator moved from 227 to 230 during the audit: the audit itself added
> `apps/api/src/common/pagination.ts` and two e2e specs while fixing defects it found.
> The inventory is regenerated rather than frozen, so Phase 3's coverage check stays
> honest — a file this work created is a file this work must also account for.
